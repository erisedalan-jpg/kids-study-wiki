# HTML 复习站交互升级 + 扩量 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给离线学生 HTML 站加纯前端（localStorage）复习能力——错题/掌握标记、进度条、Leitner 间隔复习、状态筛选、导出导入——并把词条阈值由 10 降到 3（594→约 2340）。

**Architecture:** 复习逻辑全部集中在新静态文件 `docs/student/vendor/review.js`（与 `style.css` 同为手维护静态资源，gen_html.py 不生成它，只在 `render_page` 注入 `<script>`）。`gen_html.py` 只负责：降阈值、文案随阈值、在各页注入最小 DOM 钩子（`data-stem` / 空容器 / select）。Leitner 纯逻辑做成 node 可测函数走 TDD；DOM 绑定走浏览器手验。与 `mock_review.py` 的 markdown 回写链路完全解耦（互不读写）。

**Tech Stack:** Python 3.12（`gen_html.py`，标准库 + `markdown`）、原生浏览器 JS（`review.js`，无框架）、unittest（Python 测试，`python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`）、node v24（`review.js` 纯逻辑单测 + `node --check` 语法校验）。

**Spec:** `docs/superpowers/specs/2026-05-26-HTML复习站交互升级-design.md`

---

## Shared Contract（Task 2 注入、Task 3/4 消费，名字必须一字不差对齐）

**localStorage：**
- key：`"kids-review-v1"`
- value：JSON 对象 `{ "<stem>": { "s": "ok"|"fuzzy"|"wrong", "t": <epoch-ms> } }`
- `<stem>` 同时用于词条页与真题页（stem 全局唯一）。

**Leitner 间隔（写死在 review.js）：**

| 状态 | 间隔（天） | 间隔（ms） |
|---|---:|---:|
| `wrong` | 1 | 86400000 |
| `fuzzy` | 3 | 259200000 |
| `ok` | 7 | 604800000 |
| 未标记 | — | 不进复习队列 |

到期判定：`now - t >= 间隔ms`。

**DOM 钩子（gen_html.py 注入）：**
- 词条页 / 真题页：`<h1>` 之后紧跟 `<div class="review-mark" data-stem="{stem}"></div>`（空容器，review.js 填入三按钮）。
- 科目索引页：
  - 顶部 `<div id="review-progress" data-subject="{subject}"></div>`（空，review.js 填进度条）
  - 搜索框 `<input id="review-search" class="search" placeholder="筛词条标题">`（**去掉原内联 `filterEntries` 脚本**，改由 review.js 接管）
  - 状态筛选 `<select id="review-filter">`，option 值：`all`/`wrong`/`fuzzy`/`unmarked`/`due`
  - 列表项 `<li class="{weightcls}" data-stem="{stem}" data-title="{title.lower}">`（在现有基础上**加 `data-stem`**）
- 首页 index.html：`<div id="review-io"></div>`（空，review.js 填「导出/导入」按钮 + `<input type=file>`）。
- 全站：`render_page` 头部按 depth 注入 `<script src="{prefix}/vendor/review.js" defer></script>`（prefix 复用现有 depth→`..` 逻辑）。

**review.js 暴露的 API（window.Review）：**
- `Review.STATE_INTERVALS`（间隔表对象，供测试读取）
- `Review.load()` → 读 localStorage 返回 map（坏 JSON 返回 `{}`）
- `Review.save(map)` → 写 localStorage
- `Review.mark(stem, state)` → 写单条（带时间戳），返回更新后 map
- `Review.stateOf(stem)` → `"ok"|"fuzzy"|"wrong"|null`
- `Review.isDue(stem, now)` → bool（未标记返回 false）
- `Review.mergeImport(current, incoming)` → 合并两 map（按 `t` 取较新者），返回结果（纯函数，供测试）
- `Review.exportData()` → 返回 JSON 字符串
- DOM 侧：`Review.renderMarks()` / `Review.renderProgress()` / `Review.applyFilter()` / `Review.initIO()` / `Review.init()`（DOMContentLoaded 调用）

**node 可测性约定：** review.js 末尾加
```js
if (typeof module !== "undefined" && module.exports) { module.exports = Review; }
```
并且纯函数（load/save/mergeImport/isDue/stateOf/STATE_INTERVALS）不直接触碰 `document`；`load/save` 在 `typeof localStorage === "undefined"` 时降级到一个内存对象，使 node 测试可跑。

---

### Task 1: B2 扩量——阈值默认 3 + 文案随阈值

**Files:**
- Modify: `00-元/scripts/gen_html.py`（`main` 的 `--threshold` 默认值；`gen_home`、`gen_subject_index`、`gen_top_overview` 文案）
- Test: `00-元/scripts/tests/test_gen_html.py`（新建）

- [ ] **Step 1: 写失败测试**

新建 `00-元/scripts/tests/test_gen_html.py`：
```python
import sys
import unittest
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestThresholdText(unittest.TestCase):
    def setUp(self):
        import gen_html
        self.gh = gen_html

    def test_default_threshold_is_3(self):
        # main 的 argparse 默认阈值应为 3
        import argparse
        ap = argparse.ArgumentParser()
        ap.add_argument("--threshold", type=int, default=self.gh.DEFAULT_THRESHOLD)
        self.assertEqual(self.gh.DEFAULT_THRESHOLD, 3)

    def test_home_text_uses_threshold(self):
        # gen_home 文案应反映传入阈值，而非硬编码 10
        page = self.gh.gen_home({"数学": 1, "物理": 0, "化学": 0, "生物": 0}, 1, threshold=3)
        self.assertIn("weight ≥ 3", page)
        self.assertNotIn("weight ≥ 10", page)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m unittest 00-元.scripts.tests.test_gen_html -v`（或 `cd 00-元/scripts && python -m unittest tests.test_gen_html -v`）
Expected: FAIL（`DEFAULT_THRESHOLD` 不存在 / `gen_home` 不接受 `threshold` 参数）。

- [ ] **Step 3: 实现**

在 `gen_html.py` 顶部 `SUBJECTS` 附近加：
```python
DEFAULT_THRESHOLD = 3
```
把 `main()` 里 `ap.add_argument("--threshold", type=int, default=10, ...)` 改为 `default=DEFAULT_THRESHOLD`，help 文案改「（默认 3）」。

`gen_home` 签名由 `def gen_home(stats_by_sub, total):` 改为 `def gen_home(stats_by_sub, total, threshold):`，把函数体内硬编码的「weight ≥ 10」「--threshold 10」替换为 f-string 用 `threshold`。`main()` 调用处改 `gen_home(stats, total, args.threshold)`。

`gen_subject_index` 文案（如有「weight ≥ 10」字样）同样改为读阈值——若该函数没有阈值文案则跳过。`gen_top_overview` 的「top 30」保留（与阈值无关），不改。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd 00-元/scripts && python -m unittest tests.test_gen_html -v`
Expected: PASS（2 用例）。

- [ ] **Step 5: dry-run 验证扩量规模**

Run: `python 00-元/scripts/gen_html.py --threshold 3`（不加 --apply，仅扫描）
Expected: 打印「4 科 / 约 2340 词条（weight ≥ 3）」量级（数百~两千多，远大于原 594）。记录实际数字。

- [ ] **Step 6: Commit**

```bash
git add "00-元/scripts/gen_html.py" "00-元/scripts/tests/test_gen_html.py"
git commit -m "feat(html): 学生站阈值默认降至 3 + 文案随阈值

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: gen_html.py 注入 DOM 钩子 + review.js script

**Files:**
- Modify: `00-元/scripts/gen_html.py`（`render_page`、`gen_atom_page`、`gen_exam_page`、`gen_subject_index`、`gen_home`）
- Test: `00-元/scripts/tests/test_gen_html.py`（追加用例）

- [ ] **Step 1: 写失败测试（追加到 test_gen_html.py）**

```python
class TestReviewHooks(unittest.TestCase):
    def setUp(self):
        import gen_html
        self.gh = gen_html

    def test_render_page_injects_review_js(self):
        # depth=0 与 depth=2 都应注入正确相对路径的 review.js
        p0 = self.gh.render_page("t", "<p>x</p>", depth=0)
        self.assertIn('src="./vendor/review.js"', p0)
        p2 = self.gh.render_page("t", "<p>x</p>", depth=2)
        self.assertIn('src="../../vendor/review.js"', p2)
        self.assertIn("defer", p0)

    def test_atom_page_has_mark_hook(self):
        from pathlib import Path
        fm = {"title": "加法", "weight": "12", "学科": "数学", "学段": "[小学]",
              "主题": "[数与运算]", "吉林反链": "5", "学习路径出现": "0",
              "aliases": "[加法]", "英文术语": "addition"}
        page = self.gh.gen_atom_page(Path("016-加法.md"), fm, "正文", lambda *a, **k: "")
        self.assertIn('class="review-mark"', page)
        self.assertIn('data-stem="016-加法"', page)

    def test_subject_index_has_progress_filter_and_datastem(self):
        from pathlib import Path
        atoms = [(Path("016-加法.md"),
                  {"title": "加法", "吉林反链": "5"}, 12)]
        page = self.gh.gen_subject_index("数学", atoms, {"gold": 50, "red": 20})
        self.assertIn('id="review-progress"', page)
        self.assertIn('data-subject="数学"', page)
        self.assertIn('id="review-filter"', page)
        self.assertIn('id="review-search"', page)
        self.assertIn('data-stem="016-加法"', page)
        # 原内联 filterEntries 脚本应移除（改由 review.js 接管）
        self.assertNotIn("function filterEntries", page)

    def test_home_has_review_io(self):
        page = self.gh.gen_home({"数学": 1, "物理": 0, "化学": 0, "生物": 0}, 1, threshold=3)
        self.assertIn('id="review-io"', page)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd 00-元/scripts && python -m unittest tests.test_gen_html.TestReviewHooks -v`
Expected: FAIL（钩子尚未注入）。

- [ ] **Step 3: 实现——render_page 注入 review.js**

在 `render_page`（约 246-284 行）中，`prefix` 已算好。在 `katex_head` 注入之后、`{extra_head}` 同区，往返回的 `<head>` 里加：
```python
    review_js = f'<script src="{prefix}/vendor/review.js" defer></script>'
```
并在 head 模板里加入 `{review_js}` 行（紧跟 `{katex_head}` 之后）。

- [ ] **Step 4: 实现——词条页/真题页 mark 钩子**

`gen_atom_page`（约 316 行）把 `content` 由 `f'<h1>{...title...}</h1>{meta}{body_html}'` 改为在 `<h1>` 后插入钩子：
```python
    stem = path.stem
    mark_hook = f'<div class="review-mark" data-stem="{html.escape(stem)}"></div>'
    content = f'<h1>{html.escape(title)}</h1>{mark_hook}{meta}{body_html}'
```
`gen_exam_page`（约 352 行）同样在 `<h1>` 后插入 `mark_hook`（用其 `path.stem`）。

- [ ] **Step 5: 实现——科目索引钩子 + 去内联脚本**

`gen_subject_index`（约 356-390 行）：
1. 列表项加 `data-stem`：把 `f'<li class="{cls}" data-title="...">'` 改为 `f'<li class="{cls}" data-stem="{html.escape(p.stem)}" data-title="...">'`。
2. 顶部进度占位：在 `content` 的标题行后加 `f'<div id="review-progress" data-subject="{html.escape(subject)}"></div>'`。
3. 搜索框改 id 并去内联脚本：把原 `search` 变量（含 `<input class="search" ... oninput="filterEntries...">` 和 `<script>function filterEntries...</script>`）替换为：
```python
    controls = (
        '<input id="review-search" class="search" placeholder="筛词条标题">'
        '<select id="review-filter">'
        '<option value="all">全部</option>'
        '<option value="wrong">仅错题</option>'
        '<option value="fuzzy">仅模糊</option>'
        '<option value="unmarked">未复习</option>'
        '<option value="due">今日该复习</option>'
        '</select>'
    )
```
并把 `content` 里原来拼 `{search}` 的位置改为 `{progress_div}{controls}`（`progress_div` = 上面的 `review-progress` div）。

- [ ] **Step 6: 实现——首页 review-io**

`gen_home`（约 393-416 行）在词条计数 `<p>` 后加 `'<div id="review-io"></div>'`。

- [ ] **Step 7: 跑测试确认全过**

Run: `cd 00-元/scripts && python -m unittest tests.test_gen_html -v`
Expected: PASS（Task 1 + Task 2 全部用例）。

- [ ] **Step 8: 回归全量单测**

Run: `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`
Expected: 原 124 用例 + 新增全过，无回归。

- [ ] **Step 9: Commit**

```bash
git add "00-元/scripts/gen_html.py" "00-元/scripts/tests/test_gen_html.py"
git commit -m "feat(html): gen_html 注入复习 DOM 钩子 + review.js script

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: review.js 纯逻辑（storage / Leitner / 导入合并）+ node 单测

**Files:**
- Create: `docs/student/vendor/review.js`
- Test: `docs/student/vendor/review.test.js`（node，仅本地跑，不进 student 发布包语义，但提交入库）

- [ ] **Step 1: 写失败的 node 测试**

新建 `docs/student/vendor/review.test.js`：
```js
const assert = require("assert");
const Review = require("./review.js");

// 间隔表
assert.strictEqual(Review.STATE_INTERVALS.wrong, 86400000, "wrong=1d");
assert.strictEqual(Review.STATE_INTERVALS.fuzzy, 259200000, "fuzzy=3d");
assert.strictEqual(Review.STATE_INTERVALS.ok, 604800000, "ok=7d");

// mark + stateOf（node 下走内存降级）
Review.save({});
Review.mark("016-加法", "wrong");
assert.strictEqual(Review.stateOf("016-加法"), "wrong");
assert.strictEqual(Review.stateOf("999-不存在"), null);

// isDue：刚标记的 wrong 未到期；倒退 2 天则到期
const now = Date.now();
const m = Review.load();
assert.strictEqual(Review.isDue("016-加法", now), false, "刚标记不该到期");
m["016-加法"].t = now - 2 * 86400000;
Review.save(m);
assert.strictEqual(Review.isDue("016-加法", now), true, "2天前的wrong应到期");
assert.strictEqual(Review.isDue("999-不存在", now), false, "未标记不进队列");

// mergeImport：按 t 取较新
const cur = { a: { s: "ok", t: 100 }, b: { s: "wrong", t: 100 } };
const inc = { a: { s: "wrong", t: 200 }, c: { s: "ok", t: 50 } };
const merged = Review.mergeImport(cur, inc);
assert.strictEqual(merged.a.s, "wrong", "a 取较新 t=200");
assert.strictEqual(merged.b.s, "wrong", "b 仅 current 有");
assert.strictEqual(merged.c.s, "ok", "c 仅 incoming 有");

console.log("review.test.js: ALL PASS");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node docs/student/vendor/review.test.js`
Expected: FAIL/报错（`review.js` 不存在或未导出）。

- [ ] **Step 3: 实现 review.js 纯逻辑部分**

新建 `docs/student/vendor/review.js`：
```js
/* 学生 HTML 站复习状态（纯前端 localStorage；与 mock_review.py 解耦） */
(function (global) {
  "use strict";
  var KEY = "kids-review-v1";
  var STATE_INTERVALS = { wrong: 86400000, fuzzy: 259200000, ok: 604800000 };
  var _mem = {}; // node / 无 localStorage 时的降级存储
  var hasLS = (typeof localStorage !== "undefined");

  function load() {
    if (!hasLS) return JSON.parse(JSON.stringify(_mem));
    try { return JSON.parse(localStorage.getItem(KEY) || "{}") || {}; }
    catch (e) { return {}; }
  }
  function save(map) {
    if (!hasLS) { _mem = JSON.parse(JSON.stringify(map)); return; }
    localStorage.setItem(KEY, JSON.stringify(map));
  }
  function mark(stem, state) {
    var m = load();
    if (!STATE_INTERVALS[state]) { delete m[stem]; }
    else { m[stem] = { s: state, t: Date.now() }; }
    save(m); return m;
  }
  function stateOf(stem) {
    var e = load()[stem];
    return e ? e.s : null;
  }
  function isDue(stem, now) {
    var e = load()[stem];
    if (!e || !STATE_INTERVALS[e.s]) return false;
    return (now - e.t) >= STATE_INTERVALS[e.s];
  }
  function mergeImport(current, incoming) {
    var out = {}, k;
    for (k in current) if (current.hasOwnProperty(k)) out[k] = current[k];
    for (k in incoming) if (incoming.hasOwnProperty(k)) {
      if (!out[k] || (incoming[k].t || 0) > (out[k].t || 0)) out[k] = incoming[k];
    }
    return out;
  }
  function exportData() { return JSON.stringify(load()); }

  var Review = {
    STATE_INTERVALS: STATE_INTERVALS,
    load: load, save: save, mark: mark, stateOf: stateOf,
    isDue: isDue, mergeImport: mergeImport, exportData: exportData
  };
  global.Review = Review;
  if (typeof module !== "undefined" && module.exports) module.exports = Review;
})(typeof window !== "undefined" ? window : this);
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node docs/student/vendor/review.test.js`
Expected: `review.test.js: ALL PASS`。

- [ ] **Step 5: Commit**

```bash
git add docs/student/vendor/review.js docs/student/vendor/review.test.js
git commit -m "feat(html): review.js 纯逻辑（Leitner 间隔/storage/导入合并）+ node 单测

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: review.js DOM 集成（绑定 Task 2 钩子）

**Files:**
- Modify: `docs/student/vendor/review.js`（追加 DOM 函数 + init）

- [ ] **Step 1: 实现 DOM 函数**

在 `review.js` 的 IIFE 内、`Review` 对象定义之前，加入下列函数（全部在 `typeof document === "undefined"` 时不会被 init 调用，故 node 测试不受影响）：

```js
  var STATE_LABEL = { ok: "✅已掌握", fuzzy: "⚠️模糊", wrong: "❌错题" };
  var STATE_ORDER = ["ok", "fuzzy", "wrong"];

  function renderMarks() {
    var cur = load();
    document.querySelectorAll(".review-mark").forEach(function (box) {
      var stem = box.getAttribute("data-stem");
      box.innerHTML = "";
      STATE_ORDER.forEach(function (st) {
        var b = document.createElement("button");
        b.className = "rmark-btn rmark-" + st;
        b.textContent = STATE_LABEL[st];
        if (cur[stem] && cur[stem].s === st) b.classList.add("active");
        b.addEventListener("click", function () {
          var now = (cur[stem] && cur[stem].s === st) ? null : st; // 再点取消
          mark(stem, now); renderMarks(); renderProgress();
        });
        box.appendChild(b);
      });
    });
  }

  function renderProgress() {
    var box = document.getElementById("review-progress");
    if (!box) return;
    var cur = load();
    var lis = document.querySelectorAll(".entry-list li[data-stem]");
    var total = lis.length, done = 0;
    lis.forEach(function (li) {
      var stem = li.getAttribute("data-stem");
      var st = cur[stem] ? cur[stem].s : null;
      // 行内状态色点
      var dot = li.querySelector(".rdot");
      if (!dot) { dot = document.createElement("span"); dot.className = "rdot"; li.insertBefore(dot, li.firstChild); }
      dot.className = "rdot rdot-" + (st || "none");
      if (st === "ok") done++;
    });
    var pct = total ? Math.round(done / total * 100) : 0;
    box.innerHTML = '<div class="rprog-label">已掌握 ' + done + ' / ' + total + '（' + pct + '%）</div>' +
      '<div class="rprog-bar"><div class="rprog-fill" style="width:' + pct + '%"></div></div>';
  }

  function applyFilter() {
    var q = (document.getElementById("review-search") || {}).value || "";
    var mode = (document.getElementById("review-filter") || {}).value || "all";
    q = q.trim().toLowerCase();
    var now = Date.now(), cur = load();
    document.querySelectorAll(".entry-list li[data-stem]").forEach(function (li) {
      var stem = li.getAttribute("data-stem");
      var title = li.getAttribute("data-title") || "";
      var st = cur[stem] ? cur[stem].s : null;
      var okText = (q === "" || title.indexOf(q) !== -1);
      var okMode = true;
      if (mode === "wrong") okMode = (st === "wrong");
      else if (mode === "fuzzy") okMode = (st === "fuzzy");
      else if (mode === "unmarked") okMode = (st === null);
      else if (mode === "due") okMode = isDue(stem, now);
      li.style.display = (okText && okMode) ? "" : "none";
    });
  }

  function initIO() {
    var box = document.getElementById("review-io");
    if (!box) return;
    var exp = document.createElement("button");
    exp.textContent = "导出复习记录"; exp.className = "rio-btn";
    exp.addEventListener("click", function () {
      var blob = new Blob([exportData()], { type: "application/json" });
      var a = document.createElement("a");
      a.href = URL.createObjectURL(blob);
      var d = new Date(); var ymd = d.getFullYear() +
        String(d.getMonth() + 1).padStart(2, "0") + String(d.getDate()).padStart(2, "0");
      a.download = "kids-review-" + ymd + ".json"; a.click();
      URL.revokeObjectURL(a.href);
    });
    var imp = document.createElement("input");
    imp.type = "file"; imp.accept = "application/json"; imp.className = "rio-file";
    imp.addEventListener("change", function () {
      var f = imp.files[0]; if (!f) return;
      var fr = new FileReader();
      fr.onload = function () {
        try {
          var incoming = JSON.parse(fr.result);
          save(mergeImport(load(), incoming));
          renderMarks(); renderProgress(); applyFilter();
          alert("复习记录已导入合并。");
        } catch (e) { alert("导入失败：JSON 解析错误。"); }
      };
      fr.readAsText(f);
    });
    box.appendChild(exp); box.appendChild(imp);
  }

  function init() {
    renderMarks(); renderProgress(); initIO();
    var s = document.getElementById("review-search");
    var f = document.getElementById("review-filter");
    if (s) s.addEventListener("input", applyFilter);
    if (f) f.addEventListener("change", applyFilter);
  }
```

并把这些函数加进 `Review` 对象导出：`renderMarks, renderProgress, applyFilter, initIO, init`。在 IIFE 末尾、导出之后加：
```js
  if (typeof document !== "undefined") {
    if (document.readyState !== "loading") init();
    else document.addEventListener("DOMContentLoaded", init);
  }
```

- [ ] **Step 2: node 语法校验**

Run: `node --check docs/student/vendor/review.js`
Expected: 无输出（语法 OK）。

- [ ] **Step 3: 回归纯逻辑 node 测试**

Run: `node docs/student/vendor/review.test.js`
Expected: 仍 `ALL PASS`（DOM 函数未破坏纯逻辑；node 下 init 因无 document 不触发）。

- [ ] **Step 4: Commit**

```bash
git add docs/student/vendor/review.js
git commit -m "feat(html): review.js DOM 集成（标记/进度/筛选/导出导入 + init）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: style.css 复习 UI 样式

**Files:**
- Modify: `docs/student/vendor/style.css`（追加复习 UI 段，复用现有 `--gold/--red/--blue/--grey` 变量）

- [ ] **Step 1: 追加样式**

在 `style.css` 末尾追加（变量已在 `:root` 定义，深色模式自动适配）：
```css
/* ===== 复习 UI（review.js） ===== */
.review-mark { display: flex; gap: 8px; margin: 8px 0 14px; flex-wrap: wrap; }
.rmark-btn { padding: 6px 12px; border: 1px solid var(--border); border-radius: 6px;
  background: var(--card); color: var(--fg); cursor: pointer; font-size: 0.95em; }
.rmark-btn.active.rmark-ok { background: var(--blue); color: #fff; border-color: var(--blue); }
.rmark-btn.active.rmark-fuzzy { background: var(--gold); color: #fff; border-color: var(--gold); }
.rmark-btn.active.rmark-wrong { background: var(--red); color: #fff; border-color: var(--red); }

#review-progress { margin: 10px 0; }
.rprog-label { font-size: 0.9em; color: var(--grey); margin-bottom: 4px; }
.rprog-bar { height: 8px; background: var(--border); border-radius: 4px; overflow: hidden; }
.rprog-fill { height: 100%; background: var(--blue); transition: width 0.3s; }

.rdot { display: inline-block; width: 9px; height: 9px; border-radius: 50%;
  margin-right: 6px; vertical-align: middle; background: var(--grey); }
.rdot-none { background: var(--border); }
.rdot-ok { background: var(--blue); }
.rdot-fuzzy { background: var(--gold); }
.rdot-wrong { background: var(--red); }

#review-filter { margin-left: 8px; padding: 6px; border: 1px solid var(--border);
  border-radius: 6px; background: var(--card); color: var(--fg); }
#review-io { margin: 12px 0; display: flex; gap: 10px; align-items: center; flex-wrap: wrap; }
.rio-btn { padding: 6px 12px; border: 1px solid var(--border); border-radius: 6px;
  background: var(--card); color: var(--fg); cursor: pointer; }
```

- [ ] **Step 2: 提交**

```bash
git add docs/student/vendor/style.css
git commit -m "style(html): 复习 UI 样式（标记按钮/进度条/状态点/筛选/导出）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 端到端生成 + 浏览器验收

**Files:** 无源码改动（生成产物 + 人工验收）。生成的 `docs/student/**.html` 已被 `docs/*.zip` 之外的规则跟踪——确认是否提交由控制者按现状决定（现有 student/ HTML 是入库的）。

- [ ] **Step 1: 全量生成（默认阈值 3）**

Run: `python 00-元/scripts/gen_html.py --apply`
Expected: 打印「词条 ~2340 / 真题 N / 索引 5 / 首页 1」；`ls docs/student/atoms/ | wc -l` 约 2340（远大于基线 594）。记录实际词条/真题数与生成耗时。

- [ ] **Step 2: 浏览器手动验收（spec 验收标准逐条）**

用浏览器打开 `docs/student/index.html`（file://），逐条核对：
1. 任一词条页：三按钮出现；点「❌错题」高亮；刷新后仍高亮（localStorage 持久）。
2. 对应学科索引页：该词条行显红点；顶部进度条数字随标记变化。
3. 筛选下拉「仅错题」「未复习」结果正确；搜索框与筛选叠加生效。
4. 「今日该复习」：刚标记的错题不出现（间隔 1 天未到）。临时把该条 `t` 改为 2 天前（控制台 `var m=Review.load();m['<stem>'].t=Date.now()-2*86400000;Review.save(m)` 后刷新）→ 出现。验完恢复。
5. 首页「导出复习记录」下载 JSON；控制台 `localStorage.clear()` 后用「导入」选该文件 → 状态恢复。
6. 断网 / 删除 `vendor/katex/` 后页面仍可读、标记仍可用（review.js 不依赖网络/KaTeX）。

记录每条 PASS/FAIL；任何 FAIL 回到对应 Task 修复并重测。

- [ ] **Step 3: 收尾**

确认 `git status`；按控制者决定是否提交生成的 HTML 产物（与现有 student/ 入库惯例一致）。记录最终词条/真题规模与体积变化。

---

## Self-Review

**Spec coverage：**
- B1 三态标记 → Task 2（钩子）+ Task 4（renderMarks）✅
- B1 进度条 + 状态色点 → Task 4（renderProgress）+ Task 5（样式）✅
- B1 Leitner 间隔「今日该复习」→ Task 3（isDue + 间隔表，node 测试）+ Task 4（applyFilter due 模式）✅
- B1 状态/标题筛选 → Task 2（select+search 钩子，去内联脚本）+ Task 4（applyFilter）✅
- B1 导出/导入 → Task 3（mergeImport/exportData，node 测试）+ Task 4（initIO）✅
- B2 阈值 ≥3 + 文案 → Task 1 ✅
- 与 mock_review 解耦 → review.js 只读写 localStorage，从不碰 frontmatter（Architecture 段载明）✅
- 单元边界（review.js 自包含、gen_html 改动收敛）→ Task 划分体现 ✅
- 验收标准（持久化/色点/筛选/到期/导出导入/离线）→ Task 6 Step 2 逐条 ✅
- 风险（localStorage origin 隔离→导出导入兜底；扩量体积→Task 6 记录；stem 碰撞→现状无）→ 已在 spec，Task 6 记录体积 ✅

**Placeholder scan：** 无 TBD；每个代码步骤含完整代码；每个验证步骤含确切命令与预期。

**类型/命名一致性：** Shared Contract 锁定的名字（`review-mark`/`data-stem`/`review-progress`/`review-search`/`review-filter`/`review-io`/`Review.mark/isDue/mergeImport/renderMarks/renderProgress/applyFilter/initIO/init`/`STATE_INTERVALS`/key `kids-review-v1`）在 Task 2（注入）、Task 3/4（消费）、Task 6（验收脚本）中全程一致；间隔常量 86400000/259200000/604800000 在 Shared Contract、Task 3 测试、review.js 实现三处一致。
