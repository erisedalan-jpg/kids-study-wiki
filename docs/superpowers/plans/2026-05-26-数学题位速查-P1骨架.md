# 数学题位速查 P1（骨架打印件）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 跑通「真题→三段×题位×考点频次」聚合管线，生成只含考点频次的三段题位打印优化单 HTML（浏览器 Ctrl+P 成 PDF）。

**Architecture:** 新脚本 `00-元/scripts/gen_exam_blueprint.py`：纯函数读 `真题/吉林-数学/*.md` frontmatter → 按卷型三段（08-22 旧/2023 过渡/24+ 最新）× 题位 (题型,题号) 聚合考点，经归一表合并同义后按频次排，渲染为 A4 打印 CSS 的单 HTML 到 `docs/student/数学题位速查.html`。复用 `_utils` 与现有 `vendor/katex`、`vendor/style.css`。P1 不含「公式/方法/易错」速查库（那是 P2），生成器留好挂载钩子。

**Tech Stack:** Python 3.12（标准库 + PyYAML 6.0.3，已装）、unittest（`python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`）、KaTeX（复用 `docs/student/vendor/katex/`）。

**Spec:** `docs/superpowers/specs/2026-05-26-数学题位考点速查打印件-design.md`

---

## 复用约定（实测确认）

- `from _utils import REPO_ROOT, read_frontmatter, setup_utf8`（`read_frontmatter(path)` → `dict[str,str]`，数组字段如 `考点` 是原文串 `"[三角函数, 象限]"`）。
- 真题目录：`REPO_ROOT / "真题" / "吉林-数学"`，内 `*.md`。frontmatter 字段：`年份`/`题型`(选择/填空/解答)/`题号`/`文理`(文/理/不分)/`难度`(易/中/难)/`考点`。
- YAML 读取惯例：`import yaml; yaml.safe_load(f)`。
- 输出 HTML 在 `docs/student/`（depth 0）→ 资源相对路径 `./vendor/katex/...`、`./vendor/style.css`。
- 卷型三段（spec 表）：2008–2022→`旧结构(08-22)`（仅理科）；2023→`过渡(2023)`；≥2024→`最新(24+)`。

---

### Task 1: 聚合核心纯函数

**Files:**
- Create: `00-元/scripts/gen_exam_blueprint.py`
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`

- [ ] **Step 1: 写失败测试**

Create `00-元/scripts/tests/test_gen_exam_blueprint.py`:
```python
import sys
import unittest
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestBlueprintCore(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g

    def test_parse_kaodian(self):
        self.assertEqual(self.g.parse_kaodian("[复数, 模长]"), ["复数", "模长"])
        self.assertEqual(self.g.parse_kaodian("集合"), ["集合"])
        self.assertEqual(self.g.parse_kaodian(""), [])

    def test_era_of(self):
        self.assertEqual(self.g.era_of(2008), "旧结构(08-22)")
        self.assertEqual(self.g.era_of(2022), "旧结构(08-22)")
        self.assertEqual(self.g.era_of(2023), "过渡(2023)")
        self.assertEqual(self.g.era_of(2024), "最新(24+)")
        self.assertEqual(self.g.era_of(2026), "最新(24+)")
        self.assertIsNone(self.g.era_of(2007))

    def test_difficulty_mode_tie_prefers_harder(self):
        self.assertEqual(self.g.difficulty_mode(["易", "易", "中"]), "易")
        self.assertEqual(self.g.difficulty_mode(["中", "难"]), "难")  # 并列取偏难
        self.assertEqual(self.g.difficulty_mode([]), "?")

    def test_canon_applies_normalize(self):
        norm = {"复数的运算": "复数运算", "复数运算": "复数运算"}
        self.assertEqual(self.g.canon("复数的运算", norm), "复数运算")
        self.assertEqual(self.g.canon("未知考点", norm), "未知考点")  # pass-through

    def test_aggregate_segments_and_counts(self):
        rows = [
            {"年份": "2010", "文理": "理", "题型": "选择", "题号": "2",
             "难度": "易", "考点": "[复数的运算, 共轭复数]"},
            {"年份": "2012", "文理": "理", "题型": "选择", "题号": "2",
             "难度": "易", "考点": "[复数运算]"},
            {"年份": "2011", "文理": "文", "题型": "选择", "题号": "2",
             "难度": "易", "考点": "[集合]"},  # 旧结构文卷应被过滤
            {"年份": "2024", "文理": "不分", "题型": "选择", "题号": "1",
             "难度": "易", "考点": "[复数, 模长]"},
        ]
        norm = {"复数的运算": "复数运算", "复数运算": "复数运算"}
        agg = self.g.aggregate(rows, norm)
        old = agg["旧结构(08-22)"][("选择", 2)]
        self.assertEqual(old["n"], 2)  # 文卷被过滤，只剩 2 理科题
        kp = dict(old["考点"])
        self.assertEqual(kp["复数运算"], 2)   # 复数的运算+复数运算 合并
        self.assertEqual(kp["共轭复数"], 1)
        self.assertEqual(old["难度倾向"], "易")
        self.assertIn(("选择", 1), agg["最新(24+)"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`
Expected: FAIL（模块/函数不存在）。

- [ ] **Step 3: 实现核心函数**

Create `00-元/scripts/gen_exam_blueprint.py`:
```python
"""吉林数学高考题位考点速查 — 打印优化单 HTML 生成器。

按卷型三段（08-22 旧/2023 过渡/24+ 最新）× 题位 (题型,题号) 聚合真题考点频次，
经归一表合并同义后按频次排，渲染 A4 打印 CSS 的单 HTML。

P1：只含考点频次（无公式/方法/易错速查库，那是 P2）。

CLI:
  python 00-元/scripts/gen_exam_blueprint.py                 # dry-run 打印各段题位统计
  python 00-元/scripts/gen_exam_blueprint.py --dump-考点      # 导去重考点+频次（喂归一表）
  python 00-元/scripts/gen_exam_blueprint.py --apply         # 写 docs/student/数学题位速查.html
"""
from __future__ import annotations

import argparse
import collections
import html
import sys
from pathlib import Path

import yaml

sys.path.insert(0, str(Path(__file__).parent))
from _utils import REPO_ROOT, read_frontmatter, setup_utf8  # noqa: E402

EXAM_DIR = REPO_ROOT / "真题" / "吉林-数学"
OUT_HTML = REPO_ROOT / "docs" / "student" / "数学题位速查.html"
NORMALIZE_YAML = Path(__file__).parent / "normalize_考点_数学.yaml"

ERA_ORDER = ["旧结构(08-22)", "过渡(2023)", "最新(24+)"]
TYPE_ORDER = {"选择": 0, "填空": 1, "解答": 2}
DIFF_RANK = {"易": 0, "中": 1, "难": 2}


def parse_kaodian(v: str) -> list[str]:
    v = (v or "").strip()
    if v.startswith("[") and v.endswith("]"):
        return [x.strip() for x in v[1:-1].split(",") if x.strip()]
    return [v] if v else []


def era_of(year: int) -> str | None:
    if 2008 <= year <= 2022:
        return "旧结构(08-22)"
    if year == 2023:
        return "过渡(2023)"
    if year >= 2024:
        return "最新(24+)"
    return None


def difficulty_mode(diffs: list[str]) -> str:
    if not diffs:
        return "?"
    c = collections.Counter(diffs)
    top = max(c.values())
    cands = [d for d, n in c.items() if n == top]
    # 并列取偏难（rank 大者）；未知难度排最后
    return max(cands, key=lambda d: DIFF_RANK.get(d, -1))


def canon(name: str, normalize: dict[str, str]) -> str:
    return normalize.get(name, name)


def aggregate(rows: list[dict], normalize: dict[str, str]) -> dict:
    """rows = 真题 frontmatter 字典列表 → {段: {(题型,题号): {n, 难度倾向, 考点:[(名,次)...]}}}"""
    bucket: dict = {}
    diffs: dict = {}
    counts: dict = {}
    for r in rows:
        try:
            year = int(r.get("年份") or 0)
        except ValueError:
            continue
        era = era_of(year)
        if era is None:
            continue
        if era == "旧结构(08-22)" and r.get("文理") != "理":
            continue  # 旧结构只取理科
        try:
            qno = int(r.get("题号") or 0)
        except ValueError:
            continue
        slot = (r.get("题型", ""), qno)
        key = (era, slot)
        counts[key] = counts.get(key, 0) + 1
        diffs.setdefault(key, []).append(r.get("难度", ""))
        kc = bucket.setdefault(key, collections.Counter())
        for kp in parse_kaodian(r.get("考点", "")):
            kc[canon(kp, normalize)] += 1
    out: dict = {}
    for (era, slot), kc in bucket.items():
        out.setdefault(era, {})[slot] = {
            "n": counts[(era, slot)],
            "难度倾向": difficulty_mode(diffs[(era, slot)]),
            "考点": kc.most_common(),
        }
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`
Expected: PASS（5 用例）。

- [ ] **Step 5: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py"
git commit -m "feat(blueprint): 数学题位考点聚合核心（三段/题位/归一/频次）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 真题读取 + 归一表 + --dump-考点 + CLI dry-run

**Files:**
- Modify: `00-元/scripts/gen_exam_blueprint.py`（加 read_rows / load_normalize / dump / main）
- Create: `00-元/scripts/normalize_考点_数学.yaml`（种子）
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`（追加）

- [ ] **Step 1: 追加失败测试**

Append to `test_gen_exam_blueprint.py`（在 `if __name__` 前）:
```python
class TestBlueprintIO(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g

    def test_load_normalize_missing_returns_empty(self):
        from pathlib import Path
        self.assertEqual(self.g.load_normalize(Path("/no/such/file.yaml")), {})

    def test_dump_kaodian_counts_distinct(self):
        rows = [
            {"考点": "[复数, 模长]"},
            {"考点": "[复数]"},
            {"考点": "集合"},
        ]
        d = dict(self.g.dump_kaodian(rows))
        self.assertEqual(d["复数"], 2)
        self.assertEqual(d["模长"], 1)
        self.assertEqual(d["集合"], 1)

    def test_read_rows_reads_real_exam_dir(self):
        rows = self.g.read_rows()
        self.assertGreater(len(rows), 500)  # 吉林数学 741 题
        self.assertTrue(all("年份" in r for r in rows))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintIO -v`
Expected: FAIL（read_rows/load_normalize/dump_kaodian 不存在）。

- [ ] **Step 3: 实现 IO + main**

在 `gen_exam_blueprint.py` 的 `aggregate` 之后追加：
```python
def read_rows() -> list[dict]:
    return [read_frontmatter(p) for p in sorted(EXAM_DIR.glob("*.md"))]


def load_normalize(path: Path = NORMALIZE_YAML) -> dict[str, str]:
    if not path.exists():
        return {}
    with path.open(encoding="utf-8") as f:
        data = yaml.safe_load(f) or {}
    return {str(k): str(v) for k, v in data.items()}


def dump_kaodian(rows: list[dict]) -> list[tuple[str, int]]:
    c: collections.Counter = collections.Counter()
    for r in rows:
        for kp in parse_kaodian(r.get("考点", "")):
            c[kp] += 1
    return c.most_common()


def main() -> int:
    setup_utf8()
    ap = argparse.ArgumentParser()
    ap.add_argument("--apply", action="store_true", help="写盘 HTML")
    ap.add_argument("--dump-考点", dest="dump", action="store_true",
                    help="导去重考点+频次（喂归一表）")
    args = ap.parse_args()

    rows = read_rows()
    print(f"读取 {len(rows)} 题（{EXAM_DIR.name}）", flush=True)

    if args.dump:
        for name, n in dump_kaodian(rows):
            print(f"{n:>4}  {name}")
        return 0

    normalize = load_normalize()
    agg = aggregate(rows, normalize)
    for era in ERA_ORDER:
        slots = agg.get(era, {})
        print(f"\n=== {era} ===（{len(slots)} 题位）")
        for slot in sorted(slots, key=lambda s: (TYPE_ORDER.get(s[0], 9), s[1])):
            d = slots[slot]
            top = "  ".join(f"{k}×{n}" for k, n in d["考点"][:4])
            print(f"  {slot[0]}{slot[1]:>2} (n={d['n']}, {d['难度倾向']}): {top}")

    if not args.apply:
        print("\n(dry-run) 加 --apply 写 HTML。")
        return 0

    html_out = render_html(agg)
    OUT_HTML.parent.mkdir(parents=True, exist_ok=True)
    OUT_HTML.write_text(html_out, encoding="utf-8", newline="")
    print(f"\n[APPLY] → {OUT_HTML}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

注意：`main` 引用了 `render_html`，Task 3 才实现。本任务先放一个占位以便 dry-run 跑通——在 `dump_kaodian` 后、`main` 前加临时桩：
```python
def render_html(agg: dict) -> str:  # Task 3 完整实现
    return "<!doctype html><title>占位</title>"
```

- [ ] **Step 4: 创建归一表种子**

先导出真实考点分布看长尾：
Run: `python "00-元/scripts/gen_exam_blueprint.py" --dump-考点 | head -40`
据输出 create `00-元/scripts/normalize_考点_数学.yaml`，把肉眼可见的同义合并填入（首版只需覆盖明显项，长尾后续补；未列项自动 pass-through）：
```yaml
# 数学考点归一表：原始考点名 → 规范名（未列出的 pass-through）
# 维护：python gen_exam_blueprint.py --dump-考点 看分布后补
复数的运算: 复数运算
复数运算: 复数运算
复数的几何意义: 复数的几何意义
共轭复数: 共轭复数
```
（实现者据 dump 实际输出，把诸如「XX的运算/XX运算」「XX的性质/XX性质」等明显同义项补进去，10–20 条即可，不求全。）

- [ ] **Step 5: 跑测试 + dry-run 验证**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`（全过）
Run: `python "00-元/scripts/gen_exam_blueprint.py"`（dry-run）
Expected: 打印三段，旧结构选择 1–12、填空 13–16、解答 17–22/23；最新段选择 1–11、填空 12–14、解答 15–19；每题位有样本数+难度+top 考点。

- [ ] **Step 6: 回归全量单测**

Run: `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`
Expected: 原有用例 + 新增全过。

- [ ] **Step 7: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py" "00-元/scripts/normalize_考点_数学.yaml"
git commit -m "feat(blueprint): 真题读取 + 归一表 + --dump-考点 + dry-run

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 打印优化 HTML 渲染

**Files:**
- Modify: `00-元/scripts/gen_exam_blueprint.py`（用完整 `render_html` 替换桩）
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`（追加结构断言）

- [ ] **Step 1: 追加失败测试**

Append to `test_gen_exam_blueprint.py`:
```python
class TestBlueprintHTML(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g
        self.agg = {
            "旧结构(08-22)": {("选择", 2): {"n": 40, "难度倾向": "易",
                "考点": [("复数运算", 12), ("共轭复数", 7)]}},
            "最新(24+)": {("选择", 1): {"n": 1, "难度倾向": "易",
                "考点": [("复数", 1), ("模长", 1)]}},
        }

    def test_render_has_three_era_headers_and_slots(self):
        page = self.g.render_html(self.agg)
        self.assertIn("旧结构(08-22)", page)
        self.assertIn("最新(24+)", page)
        self.assertIn("选择2", page)           # 题位标题
        self.assertIn("复数运算", page)         # 考点名
        self.assertIn("12", page)              # 频次
        self.assertIn("n=40", page)            # 样本数

    def test_render_has_print_css_and_katex(self):
        page = self.g.render_html(self.agg)
        self.assertIn("@media print", page)
        self.assertIn("break-inside", page)         # 卡片不跨页断裂
        self.assertIn("page-break-before", page)    # 段间分页
        self.assertIn("vendor/katex/katex.min.css", page)
        self.assertIn("vendor/style.css", page)

    def test_render_marks_thin_sample(self):
        page = self.g.render_html(self.agg)
        # 最新段样本=1 应有薄样本提示
        self.assertIn("样本", page)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintHTML -v`
Expected: FAIL（render_html 是桩）。

- [ ] **Step 3: 实现 render_html（替换桩）**

把 Task 2 的桩 `render_html` 替换为：
```python
PRINT_CSS = """
<style>
  body { font-family: -apple-system, "Microsoft YaHei", sans-serif; color:#222; margin:0; }
  .bp-wrap { max-width: 980px; margin: 0 auto; padding: 16px; }
  h1 { font-size: 1.5em; margin: 8px 0; }
  .era { margin-top: 18px; }
  .era > h2 { border-bottom: 2px solid #333; padding-bottom: 4px; }
  .slot { border: 1px solid #ccc; border-radius: 6px; padding: 8px 10px; margin: 8px 0;
          break-inside: avoid; page-break-inside: avoid; }
  .slot-h { font-weight: bold; margin-bottom: 4px; }
  .slot-meta { color:#666; font-weight: normal; font-size: 0.85em; }
  .kp { display: inline-block; margin: 2px 8px 2px 0; padding: 1px 6px;
        background: #f0f0f0; border-radius: 4px; font-size: 0.92em; }
  .kp b { color:#000; }
  .thin { color:#a00; font-size: 0.85em; }
  .legend { color:#666; font-size: 0.85em; margin: 6px 0 14px; }
  @media print {
    .bp-wrap { max-width: none; padding: 0; }
    .era { page-break-before: always; }
    .era:first-of-type { page-break-before: avoid; }
    .slot { break-inside: avoid; page-break-inside: avoid; }
    a { color:#000; text-decoration: none; }
  }
</style>
"""

KATEX_HEAD = """
<link rel="stylesheet" href="./vendor/katex/katex.min.css">
<script defer src="./vendor/katex/katex.min.js"></script>
<script defer src="./vendor/katex/contrib/auto-render.min.js"
  onload="renderMathInElement(document.body,{delimiters:[
    {left:'$$',right:'$$',display:true},{left:'$',right:'$',display:false}],
    throwOnError:false});"></script>
"""


def render_html(agg: dict) -> str:
    parts = []
    for era in ERA_ORDER:
        slots = agg.get(era, {})
        if not slots:
            continue
        rows_html = []
        for slot in sorted(slots, key=lambda s: (TYPE_ORDER.get(s[0], 9), s[1])):
            d = slots[slot]
            kp_html = "".join(
                f'<span class="kp"><b>{html.escape(k)}</b> ×{n}</span>'
                for k, n in d["考点"]
            )
            thin = ('<span class="thin">（样本仅 %d，规律参考）</span>' % d["n"]) \
                if d["n"] <= 2 else ""
            rows_html.append(
                f'<div class="slot"><div class="slot-h">{html.escape(slot[0])}{slot[1]}'
                f'<span class="slot-meta"> · 样本 n={d["n"]} · 难度{html.escape(d["难度倾向"])}</span>'
                f'{thin}</div>{kp_html}</div>'
            )
        parts.append(
            f'<div class="era"><h2>{html.escape(era)}</h2>' + "".join(rows_html) + "</div>"
        )
    body = "".join(parts)
    return f"""<!DOCTYPE html>
<html lang="zh-CN"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>数学题位考点速查（吉林）</title>
<link rel="stylesheet" href="./vendor/style.css">
{PRINT_CSS}{KATEX_HEAD}</head>
<body><div class="bp-wrap">
<h1>数学题位考点速查 · 吉林高考</h1>
<p class="legend">按卷型三段 × 逐题位，考点按真题频次降序。旧结构按理科卷统计；
样本 ≤2 标「规律参考」。Ctrl+P 可存 A4 PDF。</p>
{body}
</div></body></html>"""
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`
Expected: PASS（全部用例）。

- [ ] **Step 5: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py"
git commit -m "feat(blueprint): 打印优化 HTML 渲染（三段题位卡片/频次/A4 打印 CSS）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 端到端生成 + 打印验收

**Files:** 无源码改动（生成产物 + 人工打印验收）

- [ ] **Step 1: 全量生成**

Run: `python "00-元/scripts/gen_exam_blueprint.py" --apply`
Expected: 打印三段统计 + `[APPLY] → .../docs/student/数学题位速查.html`。

- [ ] **Step 2: 结构自动校验**

Run（确认三段 + 题位 + 频次 + 资源链接在产物里）:
```bash
grep -o "旧结构(08-22)\|过渡(2023)\|最新(24+)" docs/student/数学题位速查.html | sort -u
grep -c 'class="slot"' docs/student/数学题位速查.html
grep -o "vendor/katex/katex.min.css\|vendor/style.css\|@media print" docs/student/数学题位速查.html | sort -u
```
Expected: 三段名都在；slot 卡片数 = 三段题位总数（数十个）；katex/style/print CSS 均命中。

- [ ] **Step 3: 浏览器打印验收（人工）**

用浏览器 `file://` 打开 `docs/student/数学题位速查.html`：
1. 三段标题清晰；每题位卡片显「题型题号 · 样本n · 难度」+ 频次条。
2. 最新段（2024）题位标「样本仅 N，规律参考」。
3. Ctrl+P 预览 A4：段间分页（每段新起一页）、卡片不跨页断裂、黑白可读。
任一不符回到对应 Task 修。

- [ ] **Step 4: 收尾**

`git status` 确认；生成的 HTML 随现有 `docs/student/` 入库惯例由 vault backup 收（与 student 站一致）。记录 slot 卡片数与各段题位数。

---

## Self-Review

**Spec 覆盖（P1 范围）：**
- 三段卷型模型 + 旧结构仅理科 → Task 1 `era_of`/`aggregate` + 测试 ✅
- 题位 (题型,题号) 聚合 + 归一合并同义 + 频次降序 → Task 1 `aggregate`/`canon` ✅
- 难度倾向=众数(并列偏难) → Task 1 `difficulty_mode` + 测试 ✅
- 归一表 yaml + --dump-考点 种子 → Task 2 ✅
- 薄样本如实标注 → Task 3 `render_html` thin 分支 + 测试 ✅
- 打印优化单 HTML（A4/分段分页/卡片不断裂/KaTeX/复用 style.css）→ Task 3 + Task 4 ✅
- 输出 `docs/student/数学题位速查.html`、riding 进 zip → Task 2 `OUT_HTML` + Task 4 ✅
- **P1 不含速查库（公式/方法/易错）** → 生成器无该块，留 P2；spec 已声明分期，无遗漏 ✅

**占位扫描：** 无 TBD；每代码步含完整代码；归一表种子明确「据 dump 补 10–20 条」非空泛（给了具体填法与示例）。Task 2 的 `render_html` 桩是显式临时桩，Task 3 Step 3 明确替换。

**类型/命名一致性：** `parse_kaodian`/`era_of`/`difficulty_mode`/`canon`/`aggregate`/`read_rows`/`load_normalize`/`dump_kaodian`/`render_html` 跨任务签名一致；聚合中间结构 `{段:{(题型,题号):{n,难度倾向,考点:[(名,次)]}}}` 在 Task 1 产出、Task 2 dry-run 消费、Task 3 render 消费三处字段名一致（`n`/`难度倾向`/`考点`）；`ERA_ORDER`/`TYPE_ORDER`/`DIFF_RANK` 常量全局共用。
