# 数学题位速查 P2（树形脑图）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把每个题位的考点从扁平频次条升级为**缩进树形脑图**（题位 → 父主题 → 子考点，父频=子频之和，★标主导父类，未分组归「其他」），直观呈现知识从属逻辑。

**Architecture:** 新增组件 [G] 分组表 `group_考点_数学.yaml`（规范考点→父主题，人工策展）。`gen_exam_blueprint.py` 加纯函数 `build_tree`（频次列表+分组表→排序树）与 `tree_to_lines`（树→带 `├─└─│` 连接线的文本行），`main` 用它给每题位填 `树` 字段，`render_html` 把 `树` 渲染为等宽字体树块（替换原频次条），打印 CSS 加树样式。

**Tech Stack:** Python 3.12 + PyYAML（已用）、unittest（`python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`）、CSS 等宽树（打印友好，无新依赖）。

**Spec:** `docs/superpowers/specs/2026-05-26-数学题位考点速查打印件-design.md`（已含 2026-05-26 树形脑图修订）

**多模型路由：** Task 4（分组表 YAML 内容）由 **Opus 主会话**策展（数学领域判断）；Task 1/2/3/5（代码与生成）走 subagent TDD。

---

## 现状（P1 已落地，实测）

`gen_exam_blueprint.py` 已有：`aggregate(rows, normalize)` → `{段: {(题型,题号): {"n":int, "难度倾向":str, "考点":[(规范名,频次)...] 降序}}}`；`load_normalize`/`read_rows`/`dump_kaodian`/`render_html`/`main`；常量 `ERA_ORDER`/`TYPE_ORDER`/`PRINT_CSS`/`KATEX_HEAD`/`NORMALIZE_YAML`。`render_html` 当前把 `d["考点"]` 渲染成 `<span class="kp">` 频次条。

---

### Task 1: build_tree + tree_to_lines 纯函数

**Files:**
- Modify: `00-元/scripts/gen_exam_blueprint.py`（加常量 `GROUP_YAML` + 函数 `build_tree`/`tree_to_lines`）
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`（追加类）

- [ ] **Step 1: 追加失败测试**

Append to `test_gen_exam_blueprint.py`（在 `if __name__` 前）:
```python
class TestBlueprintTree(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g

    def test_build_tree_groups_sums_sorts(self):
        kp = [("复数运算", 9), ("共轭复数", 6), ("集合运算", 7),
              ("集合表示法", 4), ("一元二次不等式", 4)]
        group = {"复数运算": "复数", "共轭复数": "复数",
                 "集合运算": "集合", "集合表示法": "集合",
                 "一元二次不等式": "不等式"}
        tree = self.g.build_tree(kp, group)
        # [(父, 父频, [(子,子频)...])...]，父按频降序
        self.assertEqual(tree[0][0], "复数")
        self.assertEqual(tree[0][1], 15)            # 9+6
        self.assertEqual(tree[0][2], [("复数运算", 9), ("共轭复数", 6)])
        self.assertEqual(tree[1][0], "集合")
        self.assertEqual(tree[1][1], 11)            # 7+4
        self.assertEqual(tree[2][0], "不等式")
        self.assertEqual(tree[2][1], 4)

    def test_build_tree_ungrouped_goes_other_last(self):
        kp = [("怪考点", 5), ("复数运算", 3)]
        group = {"复数运算": "复数"}
        tree = self.g.build_tree(kp, group)
        # 复数(3) 在前；其他(5) 即便频次更高也排末
        self.assertEqual([p[0] for p in tree], ["复数", "其他"])
        self.assertEqual(tree[-1][0], "其他")
        self.assertEqual(tree[-1][2], [("怪考点", 5)])

    def test_tree_to_lines_connectors_and_star(self):
        tree = [("复数", 15, [("复数运算", 9), ("共轭复数", 6)]),
                ("集合", 11, [("集合运算", 7)])]
        lines = self.g.tree_to_lines(tree)
        self.assertEqual(lines[0], "├─ 复数 (15) ★")   # 首个父类(非其他)带★
        self.assertEqual(lines[1], "│  ├─ 复数运算 ×9")
        self.assertEqual(lines[2], "│  └─ 共轭复数 ×6")
        self.assertEqual(lines[3], "└─ 集合 (11)")       # 末父类用 └─
        self.assertEqual(lines[4], "   └─ 集合运算 ×7")  # 末父类下子项缩进无竖线

    def test_tree_to_lines_other_no_star(self):
        tree = [("其他", 5, [("怪考点", 5)])]
        lines = self.g.tree_to_lines(tree)
        self.assertEqual(lines[0], "└─ 其他 (5)")        # 仅其他时不加★
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintTree -v`
Expected: FAIL（build_tree/tree_to_lines 不存在）。

- [ ] **Step 3: 实现**

在 `gen_exam_blueprint.py` 的 `NORMALIZE_YAML = ...` 行后加常量：
```python
GROUP_YAML = Path(__file__).parent / "group_考点_数学.yaml"
OTHER = "其他"
```
在 `aggregate` 之后加两个纯函数：
```python
def build_tree(kp_freq: list[tuple[str, int]],
               group: dict[str, str]) -> list[tuple[str, int, list[tuple[str, int]]]]:
    """考点频次列表 + 分组表 → [(父主题, 父频, [(子考点,子频)...])...]。
    父按频降序，但 OTHER 恒排末；子在父下按频降序（沿用输入顺序，输入已降序）。"""
    parents: dict[str, list[tuple[str, int]]] = {}
    for name, freq in kp_freq:
        p = group.get(name, OTHER)
        parents.setdefault(p, []).append((name, freq))
    tree = []
    for p, kids in parents.items():
        tree.append((p, sum(f for _, f in kids), kids))
    # 父频降序；OTHER 永远最后（用排序键把 OTHER 压到末尾）
    tree.sort(key=lambda t: (t[0] == OTHER, -t[1]))
    return tree


def tree_to_lines(tree: list[tuple[str, int, list[tuple[str, int]]]]) -> list[str]:
    """树 → 带 ├─└─│ 连接线的文本行；首个非 OTHER 父类加 ★。"""
    lines: list[str] = []
    n = len(tree)
    for i, (parent, pfreq, kids) in enumerate(tree):
        p_last = (i == n - 1)
        p_branch = "└─" if p_last else "├─"
        star = " ★" if i == 0 and parent != OTHER else ""
        lines.append(f"{p_branch} {parent} ({pfreq}){star}")
        cn = len(kids)
        indent = "   " if p_last else "│  "
        for j, (child, cfreq) in enumerate(kids):
            c_branch = "└─" if j == cn - 1 else "├─"
            lines.append(f"{indent}{c_branch} {child} ×{cfreq}")
    return lines
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintTree -v`
Expected: PASS（4 用例）。

- [ ] **Step 5: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py"
git commit -m "feat(blueprint): build_tree + tree_to_lines（考点分组成树+连接线）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: load_group + main 填树 + 分组表种子

**Files:**
- Modify: `00-元/scripts/gen_exam_blueprint.py`（`load_group` + `main` 填 `树`）
- Create: `00-元/scripts/group_考点_数学.yaml`（最小种子；完整策展在 Task 4）
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`（追加）

- [ ] **Step 1: 追加失败测试**

```python
class TestBlueprintGroup(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g

    def test_load_group_missing_returns_empty(self):
        from pathlib import Path
        self.assertEqual(self.g.load_group(Path("/no/such.yaml")), {})

    def test_attach_trees_populates_tree_field(self):
        agg = {"旧结构(08-22)": {("选择", 1): {
            "n": 40, "难度倾向": "易",
            "考点": [("复数运算", 9), ("集合运算", 7)]}}}
        group = {"复数运算": "复数", "集合运算": "集合"}
        self.g.attach_trees(agg, group)
        slot = agg["旧结构(08-22)"][("选择", 1)]
        self.assertIn("树", slot)
        self.assertEqual(slot["树"][0][0], "复数")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintGroup -v`
Expected: FAIL（load_group/attach_trees 不存在）。

- [ ] **Step 3: 实现**

在 `gen_exam_blueprint.py` 的 `load_normalize` 之后加：
```python
def load_group(path: Path = GROUP_YAML) -> dict[str, str]:
    if not path.exists():
        return {}
    with path.open(encoding="utf-8") as f:
        data = yaml.safe_load(f) or {}
    return {str(k): str(v) for k, v in data.items()}


def attach_trees(agg: dict, group: dict[str, str]) -> None:
    """给每题位就地加 '树' 字段（build_tree(考点, group)）。"""
    for slots in agg.values():
        for d in slots.values():
            d["树"] = build_tree(d["考点"], group)
```
在 `main()` 中，`agg = aggregate(rows, normalize)` 之后、打印循环之前，加：
```python
    group = load_group()
    attach_trees(agg, group)
```
并把 dry-run 打印循环里那行 `top = "  ".join(...)` 改为打印树根（父主题）概览，便于 dry-run 验证：
```python
            roots = "  ".join(f"{p}({pf})" for p, pf, _ in d["树"][:4])
            print(f"  {slot[0]}{slot[1]:>2} (n={d['n']}, {d['难度倾向']}): {roots}")
```

- [ ] **Step 4: 创建分组表最小种子**

先看归一后考点分布：
Run: `python "00-元/scripts/gen_exam_blueprint.py" --dump-考点 | head -60`
据输出 create `00-元/scripts/group_考点_数学.yaml`（**最小种子，10–20 条明显项即可；完整策展是 Task 4**）:
```yaml
# 数学考点分组表：规范考点 → 父主题（未列出的归「其他」）
# 父主题取有限集：复数/集合/常用逻辑/不等式/函数与导数/三角函数/平面向量/
#   数列/立体几何/解析几何/概率统计/算法(程序框图)/计数原理
# 维护：python gen_exam_blueprint.py --dump-考点 看分布后挂父主题
复数运算: 复数
共轭复数: 复数
复数的几何意义: 复数
集合运算: 集合
集合表示法: 集合
一元二次不等式: 不等式
```

- [ ] **Step 5: 跑测试 + dry-run**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`（全过）
Run: `python "00-元/scripts/gen_exam_blueprint.py"`（dry-run）
Expected: 各题位打印父主题根概览（如 `选择 2 (n=42, 易): 复数(N) 集合(M) 其他(K)`）。

- [ ] **Step 6: 回归全量单测**

Run: `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`
Expected: 全过。

- [ ] **Step 7: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py" "00-元/scripts/group_考点_数学.yaml"
git commit -m "feat(blueprint): load_group + attach_trees + 分组表种子

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: render_html 树形渲染 + 树 CSS

**Files:**
- Modify: `00-元/scripts/gen_exam_blueprint.py`（`PRINT_CSS` 加树样式；`render_html` 改用 `tree_to_lines`）
- Test: `00-元/scripts/tests/test_gen_exam_blueprint.py`（改 TestBlueprintHTML）

- [ ] **Step 1: 改测试（树形断言）**

把现有 `TestBlueprintHTML` 类整体替换为（fixture 加 `树` 字段；断言树行与树 CSS）:
```python
class TestBlueprintHTML(unittest.TestCase):
    def setUp(self):
        import gen_exam_blueprint as g
        self.g = g
        self.agg = {
            "旧结构(08-22)": {("选择", 2): {"n": 40, "难度倾向": "易",
                "考点": [("复数运算", 12), ("共轭复数", 7)],
                "树": [("复数", 19, [("复数运算", 12), ("共轭复数", 7)])]}},
            "最新(24+)": {("选择", 1): {"n": 1, "难度倾向": "易",
                "考点": [("复数", 1)],
                "树": [("复数", 1, [("复数", 1)])]}},
        }

    def test_render_has_eras_and_tree_lines(self):
        page = self.g.render_html(self.agg)
        self.assertIn("旧结构(08-22)", page)
        self.assertIn("最新(24+)", page)
        self.assertIn("选择2", page)
        self.assertIn("复数运算", page)
        self.assertIn("├─", page)            # 树连接线
        self.assertIn("(19)", page)           # 父主题频次
        self.assertIn("★", page)              # 主导父类标记
        self.assertIn("n=40", page)

    def test_render_has_print_css_katex_and_tree_css(self):
        page = self.g.render_html(self.agg)
        self.assertIn("@media print", page)
        self.assertIn("break-inside", page)
        self.assertIn("page-break-before", page)
        self.assertIn("vendor/katex/katex.min.css", page)
        self.assertIn("vendor/style.css", page)
        self.assertIn("bp-tree", page)        # 树容器样式
        self.assertIn("white-space: pre", page)  # 等宽对齐

    def test_render_marks_thin_sample(self):
        page = self.g.render_html(self.agg)
        self.assertIn("（样本仅 1，规律参考）", page)
        self.assertNotIn("（样本仅 40，规律参考）", page)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint.TestBlueprintHTML -v`
Expected: FAIL（render 还是频次条，无 `├─`/`bp-tree`）。

- [ ] **Step 3: 实现**

在 `PRINT_CSS` 的 `<style>` 内（`.kp` 规则附近）加树样式：
```css
  .bp-tree { font-family: "Cascadia Mono", Consolas, "Microsoft YaHei", monospace;
    white-space: pre; font-size: 0.9em; line-height: 1.55; margin: 4px 0 0; }
  .bp-tree .tline { margin: 0; }
```
把 `render_html` 中每题位拼 `kp_html` 的那段：
```python
            kp_html = "".join(
                f'<span class="kp"><b>{html.escape(k)}</b> ×{n}</span>'
                for k, n in d["考点"]
            )
```
替换为用树行渲染：
```python
            tree_lines = tree_to_lines(d["树"])
            kp_html = '<div class="bp-tree">' + "".join(
                f'<div class="tline">{html.escape(line)}</div>' for line in tree_lines
            ) + '</div>'
```
（变量名 `kp_html` 保留不变，后面 slot 卡片拼接处无需改。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "00-元/scripts" && python -m unittest tests.test_gen_exam_blueprint -v`
Expected: PASS（全部）。

- [ ] **Step 5: 回归全量单测**

Run: `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`
Expected: 全过。

- [ ] **Step 6: Commit**

```bash
git add "00-元/scripts/gen_exam_blueprint.py" "00-元/scripts/tests/test_gen_exam_blueprint.py"
git commit -m "feat(blueprint): render_html 树形脑图渲染（连接线/父频/★ + 等宽树 CSS）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 策展完整分组表（Opus 主会话）

> **此任务由 Opus 主会话执行**（数学领域判断；非 subagent）。

**Files:**
- Modify: `00-元/scripts/group_考点_数学.yaml`（扩到覆盖高频考点）

- [ ] **Step 1: 导出归一后考点全量分布**

Run: `python "00-元/scripts/gen_exam_blueprint.py" --dump-考点`
得到「规范考点 + 频次」全表（降序）。

- [ ] **Step 2: 把高频考点挂到父主题**

把频次靠前、覆盖各题位主干的考点（建议覆盖到累计频次约 80% 或前 ~120 个）映射到父主题。父主题限定有限集：
`复数 / 集合 / 常用逻辑 / 不等式 / 函数与导数 / 三角函数 / 平面向量 / 数列 / 立体几何 / 解析几何 / 概率统计 / 算法(程序框图) / 计数原理 / 数学文化 / 实际应用`（如需新增父主题，保持粗粒度、总数 ≤ ~16）。
原则：同一数学条线的考点归同一父主题；拿不准的低频考点留空（自动归「其他」）；不强求全覆盖。

- [ ] **Step 3: 校验 YAML 合法 + 无跨条线误挂**

Run: `python -c "import yaml; d=yaml.safe_load(open('00-元/scripts/group_考点_数学.yaml',encoding='utf-8')); print('条目', len(d)); import collections; print(collections.Counter(d.values()))"`
Expected: 打印映射条数 + 各父主题分布；人眼复核无明显跨条线误挂（如「离心率」不应挂到「复数」）。

- [ ] **Step 4: Commit**

```bash
git add "00-元/scripts/group_考点_数学.yaml"
git commit -m "feat(blueprint): 策展数学考点分组表（高频考点挂父主题）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 端到端生成 + 打印验收

**Files:** 无源码改动（生成产物 + 人工验收）

- [ ] **Step 1: 全量生成**

Run: `python "00-元/scripts/gen_exam_blueprint.py" --apply`
Expected: `[APPLY] → docs/student/数学题位速查.html`。

- [ ] **Step 2: 结构自动校验**

Run:
```bash
grep -o "旧结构(08-22)\|过渡(2023)\|最新(24+)" docs/student/数学题位速查.html | sort -u
grep -o 'class="bp-tree"' docs/student/数学题位速查.html | wc -l   # = 题位数 65
grep -o '├─\|└─' docs/student/数学题位速查.html | wc -l            # 树连接线大量
grep -o "★" docs/student/数学题位速查.html | wc -l                 # 各题位主导父类
grep -o "white-space: pre\|@media print\|break-inside" docs/student/数学题位速查.html | sort -u
```
Expected: 三段齐；bp-tree 数 = 65；连接线与 ★ 数量合理（★ ≈ 有非「其他」父类的题位数）。

- [ ] **Step 3: 浏览器打印验收（人工）**

`file://` 打开 `docs/student/数学题位速查.html`，Ctrl+P 预览 A4：
1. 每题位呈缩进树：父主题（带频次，主导项 ★）→ 子考点（带 ×频次），连接线 `├─└─│` 对齐。
2. 旧结构选择2 应见「复数」为主导父类；选择11 见「解析几何」（离心率/双曲线）等。
3. 「其他」父类排在各题位末尾。
4. 段间分页、卡片不跨页断裂、等宽树对齐、黑白可读。
任一不符回对应 Task 修。

- [ ] **Step 4: 收尾**

`git status` 确认；生成 HTML 随 `docs/student/` 入库惯例由 vault backup 收。记录各题位树根（父主题）分布抽样。

---

## Self-Review

**Spec 覆盖（P2 范围）：**
- 新增 [G] 分组表（规范考点→父主题，未分组归其他）→ Task 2 种子 + Task 4 完整策展 ✅
- build_tree：父频=子频之和、父按频降序、OTHER 末位 → Task 1 + 测试 ✅
- tree_to_lines：├─└─│ 连接线、首个非 OTHER 父类 ★ → Task 1 + 测试 ✅
- 中间结构升级加「树」字段 → Task 2 `attach_trees` + 测试 ✅
- render_html 改缩进树形脑图（替换频次条）+ 等宽树 CSS → Task 3 + 测试 ✅
- 2 层深度（父→子）→ build_tree 结构即 2 层 ✅
- 打印友好（连接线非颜色、卡片不断裂、A4 分页）→ Task 3 CSS + Task 5 验收 ✅
- 多模型路由：分组表 Opus 策展 → Task 4 明确标注主会话执行 ✅
- 薄样本提示保留 → Task 3 TestBlueprintHTML 保留 thin 断言 ✅

**占位扫描：** 无 TBD；每代码步含完整代码；分组表种子（Task 2）与完整策展（Task 4）分开，Task 4 给了父主题集合 + 覆盖目标 + 校验法，非空泛。

**类型/命名一致性：** `build_tree`/`tree_to_lines`/`load_group`/`attach_trees`/`GROUP_YAML`/`OTHER` 跨任务一致；树结构 `[(父,父频,[(子,子频)...])...]` 在 Task1 产出、Task2 attach、Task3 render、测试四处字段/形状一致；`render_html` 仍 `(agg)` 单参（树预存在 `d["树"]`），变量名 `kp_html` 复用避免改卡片拼接处；`d["树"]` 字段名与 spec 中间结构一致。