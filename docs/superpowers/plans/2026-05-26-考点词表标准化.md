# 考点命名规范与词表标准化（保守）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 立考点命名规范，源头只归并纯写法变体改写吉林-数学 741 真题 `考点` 字段，温和重算下游并验证 weight 漂移极小。

**Architecture:** 4 组件——命名规则文档 + 变体映射 YAML（Opus 策展）+ 迁移脚本（TDD，幂等、git 干净基线守卫、dry-run/diff/--apply，只改 frontmatter `考点` 行、仅写真正变更的文件）+ 下游重算链（exam_index→compute_weight→gen_html→gen_exam_blueprint）含 weight 漂移对比与孤儿反链检查。

**Tech Stack:** Python 3.12 + PyYAML、unittest（`python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py"`）。

**Spec:** `docs/superpowers/specs/2026-05-26-考点命名规范与词表标准化-design.md`

**多模型路由：** Task 1（规则文档）、Task 2（变体映射策展）由 **Opus 主会话**（数学/用字判断）；Task 3（迁移脚本代码）走 subagent TDD；Task 4/5（迁移执行 + 重算验证）主会话执行、带人工过 diff 关卡。

---

## 复用约定（实测确认）

- 真题 frontmatter `考点:` 行形如 `考点: [三角函数, 象限]`（偶有单值 `考点: 集合`）。`考点` 仅出现在 frontmatter，正文不以行首 `考点:` 出现。
- 下游 CLI：`exam_index.py --province 吉林 --subject 数学`；`compute_weight.py --subject 数学 --apply`（`--dry-run` 预览）；`gen_html.py --apply`；`gen_exam_blueprint.py --apply`。
- 考点 → 反链（exam_index 按 alias 匹配概念词条，写 `<!-- exam-backlinks-start/end -->` 段）→ weight（compute_weight 读反链段）。
- 全部考点 tag 已导出 `docs/superpowers/working/_kaodian_by_name.txt`（922 个，按名排序）。

---

### Task 1: 考点命名规则文档（Opus 主会话）

> 由 Opus 主会话执行。

**Files:** Create `00-元/考点命名规则.md`

- [ ] **Step 1: 写规则文档**

写入 spec 的 6 条规则（去「的」/操作词归一/「图象」统一/去赘尾/不并语义项/受控词表）+「纯变体硬标准」+「如何加新考点」流程（新真题考点须查 canonical 清单；确需新词在 `canonical_考点_数学.yaml` 加映射并记录）。文档须可执行、与 CLAUDE.md 红线风格一致（明确"一字不动"约束语气）。

- [ ] **Step 2: Commit**

```bash
git add "00-元/考点命名规则.md"
git commit -m "docs: 考点命名规范（6 条 + 纯变体硬标准 + 加词流程）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 变体映射表策展（Opus 主会话）

> 由 Opus 主会话执行（逐条确认满足"纯变体硬标准"）。

**Files:** Create `00-元/scripts/canonical_考点_数学.yaml`

- [ ] **Step 1: 按 6 规则聚类全表**

依据 `_kaodian_by_name.txt`（922 个，已按名排序，近义自然聚类），逐簇判定：
- 满足硬标准（去结构词/图象异写后字面同义）→ 入映射 `变体: 规范名`。
- 语义不同（复数乘法 vs 除法、正弦 vs 余弦定理、线面平行 vs 垂直）→ **不入表**。
- 拿不准 → 不入表（保守 pass-through）。

文件形如：
```yaml
# 考点变体→规范名映射（仅纯写法变体，满足命名规则硬标准）。未列出的 pass-through。
# 维护见 00-元/考点命名规则.md
集合的表示法: 集合表示法
集合表示法: 集合表示法
集合的运算: 集合运算
集合运算: 集合运算
交集: 交集运算
交集及其运算: 交集运算
集合交集: 交集运算
集合的交集: 交集运算
集合的交集运算: 交集运算
并集: 并集运算
并集运算: 并集运算
三角函数的图像与性质: 三角函数图象与性质
三角函数的图象与性质: 三角函数图象与性质
复数的运算: 复数运算
函数的奇偶性: 函数奇偶性
导数的几何意义: 导数几何意义
命题的真假判断: 命题真假判断
命题的真假判断与应用: 命题真假判断
绝对值不等式的解法: 绝对值不等式
# … 覆盖各簇
```

- [ ] **Step 2: 自查无误并**

`python -c "import yaml; d=yaml.safe_load(open('00-元/scripts/canonical_考点_数学.yaml',encoding='utf-8')); print('映射',len(d),'规范名',len(set(d.values())))"`
人眼复核：每条左右确为同义；无语义不同项被并（重点查 复数*/正余弦/线面*/椭圆双曲线 等）。

- [ ] **Step 3: Commit**

```bash
git add "00-元/scripts/canonical_考点_数学.yaml"
git commit -m "feat: 考点变体→规范名映射（保守，仅纯写法变体）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 迁移脚本 normalize_kaodian_source.py（TDD）

**Files:**
- Create: `00-元/scripts/normalize_kaodian_source.py`
- Test: `00-元/scripts/tests/test_normalize_kaodian_source.py`

- [ ] **Step 1: 写失败测试**

Create `00-元/scripts/tests/test_normalize_kaodian_source.py`:
```python
import sys
import unittest
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestNormalizeSource(unittest.TestCase):
    def setUp(self):
        import normalize_kaodian_source as m
        self.m = m

    def test_map_tags_dedup_preserves_order(self):
        m = {"交集": "交集运算", "集合的交集运算": "交集运算"}
        self.assertEqual(self.m.map_tags(["交集", "集合的交集运算", "并集"], m),
                         ["交集运算", "并集"])  # 两变体并为一，去重保序

    def test_map_tags_passthrough_unmapped(self):
        self.assertEqual(self.m.map_tags(["复数除法", "复数乘法"], {}),
                         ["复数除法", "复数乘法"])

    def test_rewrite_changes_only_kaodian_line(self):
        text = ("---\n"
                "title: 2010-理-01\n"
                "考点: [集合的表示法, 复数除法]\n"
                "难度: 易\n"
                "---\n\n正文 考点: 不应被改\n")
        cmap = {"集合的表示法": "集合表示法"}
        new, before, after = self.m.rewrite_text(text, cmap)
        self.assertEqual(before, ["集合的表示法", "复数除法"])
        self.assertEqual(after, ["集合表示法", "复数除法"])
        self.assertIn("考点: [集合表示法, 复数除法]", new)
        self.assertIn("title: 2010-理-01", new)   # 其余 frontmatter 不动
        self.assertIn("难度: 易", new)
        self.assertIn("正文 考点: 不应被改", new)   # 正文不动

    def test_rewrite_noop_when_no_change(self):
        text = "---\ntitle: x\n考点: [复数除法]\n---\n"
        new, before, after = self.m.rewrite_text(text, {})
        self.assertEqual(before, after)
        self.assertEqual(new, text)   # 无变更则原样返回（不重排格式）

    def test_rewrite_single_value(self):
        text = "---\n考点: 集合的表示法\n---\n"
        new, before, after = self.m.rewrite_text(text, {"集合的表示法": "集合表示法"})
        self.assertEqual(after, ["集合表示法"])
        self.assertIn("考点: [集合表示法]", new)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "00-元/scripts" && python -m unittest tests.test_normalize_kaodian_source -v`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现**

Create `00-元/scripts/normalize_kaodian_source.py`:
```python
"""真题 考点 词表标准化迁移：按变体映射改写 frontmatter 考点 字段（保守）。

只动 frontmatter `考点:` 行；只写真正变更的文件；幂等。
--apply 前要求 git 工作树干净（基线可回退）。

CLI:
  python 00-元/scripts/normalize_kaodian_source.py            # dry-run：列将改文件+前后diff
  python 00-元/scripts/normalize_kaodian_source.py --apply    # 写盘（需 git 干净）
"""
from __future__ import annotations

import argparse
import re
import subprocess
import sys
from pathlib import Path

import yaml

sys.path.insert(0, str(Path(__file__).parent))
from _utils import REPO_ROOT, setup_utf8  # noqa: E402

EXAM_DIR = REPO_ROOT / "真题" / "吉林-数学"
CANON_YAML = Path(__file__).parent / "canonical_考点_数学.yaml"

FM_RE = re.compile(r"^(---\s*\n)(.*?)(\n---\s*\n)", re.DOTALL)
KAO_RE = re.compile(r"^(考点:[ \t]*)(.*)$", re.MULTILINE)


def load_canonical(path: Path = CANON_YAML) -> dict[str, str]:
    if not path.exists():
        return {}
    with path.open(encoding="utf-8") as f:
        data = yaml.safe_load(f) or {}
    return {str(k): str(v) for k, v in data.items()}


def _parse_list(v: str) -> list[str]:
    v = (v or "").strip()
    if v.startswith("[") and v.endswith("]"):
        return [x.strip() for x in v[1:-1].split(",") if x.strip()]
    return [v] if v else []


def map_tags(tags: list[str], cmap: dict[str, str]) -> list[str]:
    out: list[str] = []
    seen: set[str] = set()
    for t in tags:
        c = cmap.get(t, t)
        if c not in seen:
            seen.add(c)
            out.append(c)
    return out


def rewrite_text(text: str, cmap: dict[str, str]) -> tuple[str, list[str], list[str]]:
    """改写首个 frontmatter 内的 考点 行。返回 (新文本, before, after)。无变更则原样返回。"""
    fm = FM_RE.match(text)
    if not fm:
        return text, [], []
    head, fm_body, tail = fm.group(1), fm.group(2), fm.group(3)
    captured: dict[str, list[str]] = {"before": [], "after": []}

    def repl(km: re.Match) -> str:
        prefix, val = km.group(1), km.group(2)
        before = _parse_list(val)
        after = map_tags(before, cmap)
        captured["before"] = before
        captured["after"] = after
        return f"{prefix}[{', '.join(after)}]"

    new_body = KAO_RE.sub(repl, fm_body, count=1)
    before, after = captured["before"], captured["after"]
    if before == after:
        return text, before, after  # 无变更：原文照返，不重排格式
    new_text = text[:fm.start()] + head + new_body + tail + text[fm.end():]
    return new_text, before, after


def git_clean() -> bool:
    r = subprocess.run(["git", "status", "--porcelain"],
                       cwd=REPO_ROOT, capture_output=True, text=True)
    return r.returncode == 0 and r.stdout.strip() == ""


def main() -> int:
    setup_utf8()
    ap = argparse.ArgumentParser()
    ap.add_argument("--apply", action="store_true", help="写盘（需 git 干净）")
    args = ap.parse_args()

    cmap = load_canonical()
    if not cmap:
        print(f"映射表空或不存在：{CANON_YAML}"); return 1

    if args.apply and not git_clean():
        print("拒绝：git 工作树不干净，先提交/暂存以保基线。"); return 1

    changed = 0
    for p in sorted(EXAM_DIR.glob("*.md")):
        text = p.read_text(encoding="utf-8")
        new, before, after = rewrite_text(text, cmap)
        if new == text:
            continue
        changed += 1
        print(f"{p.name}: {before} → {after}")
        if args.apply:
            p.write_text(new, encoding="utf-8", newline="")
    print(f"\n{'[APPLY] 已改写' if args.apply else '[dry-run] 将改写'} {changed} 文件")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "00-元/scripts" && python -m unittest tests.test_normalize_kaodian_source -v`
Expected: PASS（5 用例）。

- [ ] **Step 5: 回归全量单测**

Run: `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py" 2>&1 | tail -3`
Expected: 全过。

- [ ] **Step 6: Commit**

```bash
git add "00-元/scripts/normalize_kaodian_source.py" "00-元/scripts/tests/test_normalize_kaodian_source.py"
git commit -m "feat: 考点词表标准化迁移脚本（幂等/git守卫/dry-run/只改考点行）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 迁移执行（主会话，带人工过 diff 关卡）

> 主会话执行；`--apply` 前人工过 dry-run diff。

**Files:** 改写 `真题/吉林-数学/*.md` 的 考点 字段

- [ ] **Step 1: weight 基线快照**

Run（迁移前导出全数学词条 weight 作基线）:
```bash
grep -rH "^weight:" 数学/ | sort > docs/superpowers/working/_weight_before.txt
wc -l docs/superpowers/working/_weight_before.txt
```

- [ ] **Step 2: 确认 git 干净 + dry-run 过 diff**

Run: `git status --porcelain | head`（应空，前序任务已各自提交）
Run: `python "00-元/scripts/normalize_kaodian_source.py" 2>&1 | tee docs/superpowers/working/_migrate_dryrun.txt | tail -40`
人工过 diff：每条 `before → after` 确为纯变体合并、无语义项误并。如发现误并 → 回 Task 2 修映射表，重跑 dry-run。

- [ ] **Step 3: apply**

Run: `python "00-元/scripts/normalize_kaodian_source.py" --apply 2>&1 | tail -10`
Run: `python "00-元/scripts/gen_exam_blueprint.py" --dump-考点 | wc -l`（不同考点数应显著少于 922）

- [ ] **Step 4: Commit 迁移**

```bash
git add 真题/吉林-数学/
git commit -m "data: 吉林-数学 考点词表标准化（纯变体归并，N 文件）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 下游重算 + weight 漂移验证（主会话）

> 主会话执行。

**Files:** 重算产物（反链/weight/HTML/脑图）

- [ ] **Step 1: 重建反链 + 索引**

Run: `python "00-元/scripts/exam_index.py" --province 吉林 --subject 数学 2>&1 | tail -15`
检查孤儿反链：列出真题缺口考点后，确认无因改名残留的旧考点反链文件异常（如有明显孤儿 `学科/<旧名>.md` 仅含空反链段，记录待清）。

- [ ] **Step 2: 重算 weight + 漂移对比**

Run: `python "00-元/scripts/compute_weight.py" --subject 数学 --apply 2>&1 | tail -10`
Run（迁移后快照 + diff）:
```bash
grep -rH "^weight:" 数学/ | sort > docs/superpowers/working/_weight_after.txt
diff docs/superpowers/working/_weight_before.txt docs/superpowers/working/_weight_after.txt | tee docs/superpowers/working/_weight_drift.txt | head -40
echo "漂移行数: $(grep -cE '^[<>]' docs/superpowers/working/_weight_drift.txt)"
```
验收：漂移词条数少、幅度小；逐条可解释（变体合并后匹配/不匹配概念词条）。**若大批 weight 跳变 → 停，回 Task 2 查误并。**

- [ ] **Step 3: 刷新 HTML + 脑图**

Run: `python "00-元/scripts/gen_html.py" --apply 2>&1 | tail -3`
Run: `python "00-元/scripts/gen_exam_blueprint.py" --apply 2>&1 | tail -3`

- [ ] **Step 4: 验证 + Commit 重算产物**

抽查：`--dump-考点` 中集合/三角图象/复数运算等簇已收敛为单一规范名。单测回归 `python -m unittest discover -s "00-元/scripts/tests" -p "test_*.py" 2>&1 | tail -3`。
```bash
git add 数学/ 索引/ docs/student/
git commit -m "data: 考点标准化后重算（反链/weight/HTML/脑图），weight 漂移极小

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 5: 记录衔接**

记录最终不同考点数、weight 漂移行数。X 完成——下一步回题位脑图，把 `group_考点_数学.yaml` 改作用在 canonical 词表上、完成父主题分组（另起计划）。

---

## Self-Review

**Spec 覆盖：**
- 命名规范 6 条 + 硬标准 + 加词流程 → Task 1 ✅
- 变体映射（仅纯变体、保守）→ Task 2 ✅
- 迁移脚本（只改考点行/幂等/git守卫/dry-run/只写变更文件）→ Task 3 + 测试 ✅
- 安全（git 干净基线 + dry-run 过 diff + weight 快照）→ Task 3 git_clean + Task 4 Step1/2 ✅
- 下游重算链（exam_index→compute_weight→gen_html→blueprint）+ weight 漂移对比 + 孤儿反链检查 → Task 5 ✅
- 验证（考点碎片下降、frontmatter 其余不动、漂移可解释、单测回归）→ Task 3 测试 + Task 4 Step3 + Task 5 ✅
- 范围限吉林-数学 → 全程 EXAM_DIR 固定 ✅
- 与脑图衔接 → Task 5 Step5 ✅

**占位扫描：** 无 TBD；脚本/测试代码完整；Task 2 映射给了硬标准 + 示例 + 自查法（完整表是 Opus 执行产出，非计划占位）。

**类型/命名一致性：** `load_canonical`/`map_tags`/`rewrite_text`/`git_clean` 跨任务一致；`rewrite_text` 返回 `(新文本, before, after)` 在脚本与测试一致；无变更原样返回（`before==after` 守卫）在测试 `test_rewrite_noop_when_no_change` 锁定；下游命令与实测 CLI 一致（`--province 吉林 --subject 数学` / `--subject 数学 --apply`）。
