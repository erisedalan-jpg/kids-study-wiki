# 考点专题复习材料生成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为每个 canonical 考点（沿父主题树）聚合其全部真题并整合知识，生成考生复习用的 `复习/<科>/<考点>.md` 专题树 + HTML。

**Architecture:** 三层流水线 —— ①`extract_exam_text.py` 从解析卷 PDF 按题抽题干/解析文本写回真题 md（可复用语料层）；②`gen_kaodian_review.py` 按考点聚合语料+树tag+概念词条，调 DeepSeek v4-pro 生成专题 md，`validate_review.py` 体检门把关；③`gen_html.py` 扩展渲染复习树。数学先行（P0 语料→P1 闭环），再参数化扩物化生（P2）。

**Tech Stack:** Python 3.12 + pypdf + PyYAML + unittest；既有 `_llm_router`（DeepSeek 调用：`call(prompt, task=Task.COMPLEX, system=...)`）、`_utils`（`REPO_ROOT/setup_utf8/read_frontmatter/iter_entries/bare_name`）、`fix_wikilinks`/`fix_latex_delim`。

**spec：** `docs/superpowers/specs/2026-05-27-考点专题复习材料生成-design.md`

**前置事实（实现者须知）：**
- 真题 md：`真题/{省}-{科}/*.md`，frontmatter 含 `考点`(list,已归一 canonical)/`难度`/`题型`/`题号`/`年份`/`PDF`(相对仓库根路径)/`PDF页码`(int)；正文已有 `## 摘要`/`## 关联考点`/`## 答案与解析`。
- 解析卷 PDF 含可抽取文本层（已验证）；`pypdf.PdfReader(path).pages[页码-1].extract_text()`。
- 树：`00-元/scripts/group_考点_<科>.yaml`（`考点: 父主题`）。
- LLM：`from _llm_router import call, Task`；`r = call(prompt=..., task=Task.COMPLEX, system=...)`；`r.text`。
- 命令一律 `PYTHONIOENCODING=utf-8`（Windows 控制台中文）。所有脚本放 `00-元/scripts/`，测试放 `00-元/scripts/tests/`，`sys.path.insert(0, str(Path(__file__).resolve().parents[1]))` 后裸 import。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `00-元/scripts/exam_text_segment.py` | 纯函数：页文本 + 题号 → (题干, 解析)。无 IO，可纯测。 |
| `00-元/scripts/extract_exam_text.py` | P0 主程序：遍历真题→读 PDF 页→切分→写 md 区块。CLI/幂等/dry-run。 |
| `00-元/scripts/kaodian_aggregate.py` | 纯函数：聚合 考点→{真题, 父主题, 题位, 难度分布, 年份, 词条}。 |
| `00-元/scripts/_prompts/kaodian_review.md` | 考点专题生成提示词模板。 |
| `00-元/scripts/gen_kaodian_review.py` | P1 主程序：按考点组 prompt→`call`→写 `复习/<科>/<考点>.md`。 |
| `00-元/scripts/validate_review.py` | P1 体检门：复习 md 字段/区块/公式定界符/wikilinks 校验。 |
| `00-元/scripts/gen_html.py` | 扩展：渲染 `复习/` 树（修改既有文件）。 |
| `复习/<科>/<考点>.md` | 产物（数据，非脚本）。 |

---

## P0 — 真题文本语料（数学先行）

### Task P0.1: 题文本切分纯函数

**Files:**
- Create: `00-元/scripts/exam_text_segment.py`
- Test: `00-元/scripts/tests/test_exam_text_segment.py`

- [ ] **Step 1: 写失败测试**

```python
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestSegment(unittest.TestCase):
    def setUp(self):
        import exam_text_segment as s
        self.s = s

    def test_segments_question_and_solution(self):
        page = (
            "1．设集合 A={1,2}，则 A 的子集个数为（ ）\n"
            "A．2  B．3  C．4  D．8\n"
            "【答案】C\n"
            "【解析】子集个数为 2^n=4。\n"
            "2．复数 z=1+i 的模为（ ）\n"
            "【答案】B\n"
        )
        out = self.s.segment_by_qno(page, 1)
        self.assertIn("子集个数", out["题干"])
        self.assertNotIn("复数 z", out["题干"])      # 不串入下一题
        self.assertIn("2^n=4", out["解析"])
        self.assertNotIn("【答案】", out["题干"])     # 答案/解析归入解析段

    def test_missing_qno_returns_fallback(self):
        page = "无编号锚点的整页文本"
        out = self.s.segment_by_qno(page, 7)
        self.assertEqual(out["题干"], "")
        self.assertEqual(out["解析"].strip(), "无编号锚点的整页文本")
        self.assertTrue(out["fallback"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_exam_text_segment -v`
Expected: FAIL（`No module named exam_text_segment`）

- [ ] **Step 3: 实现**

```python
# exam_text_segment.py
"""真题页文本 → (题干, 解析) 纯切分（无 IO，便于单测）。

策略：在页文本里以「行首题号」为边界切出本题块（题号 N 到 N+1），
块内以 答案/解析 标记（【答案】【解析】【详解】解：）切分题干与解析。
切分失败（找不到本题号锚点）→ fallback：整页归解析段。
"""
from __future__ import annotations

import re

_ANS_MARK = re.compile(r"(【答案】|【解析】|【详解】|^解[：:])", re.M)


def _qno_anchor(qno: int) -> re.Pattern:
    # 行首题号：1．/ 1. / 1、（全/半角点顿号），允许前导空白
    return re.compile(rf"^\s*{qno}\s*[．.、]", re.M)


def segment_by_qno(page_text: str, qno: int) -> dict:
    """返回 {题干, 解析, fallback}。"""
    m = _qno_anchor(qno).search(page_text)
    if not m:
        return {"题干": "", "解析": page_text.strip(), "fallback": True}
    start = m.start()
    nxt = _qno_anchor(qno + 1).search(page_text, m.end())
    block = page_text[start: nxt.start() if nxt else len(page_text)]
    am = _ANS_MARK.search(block)
    if am:
        stem, sol = block[: am.start()], block[am.start():]
    else:
        stem, sol = block, ""
    return {"题干": stem.strip(), "解析": sol.strip(), "fallback": False}
```

- [ ] **Step 4: 运行验证通过**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_exam_text_segment -v`
Expected: PASS（2 tests）

- [ ] **Step 5: 提交**

```bash
git add 00-元/scripts/exam_text_segment.py 00-元/scripts/tests/test_exam_text_segment.py
git commit -m "feat(review): 真题页文本题号切分纯函数 + 测试"
```

### Task P0.2: extract_exam_text.py 主程序

**Files:**
- Create: `00-元/scripts/extract_exam_text.py`
- Test: `00-元/scripts/tests/test_extract_exam_text.py`

- [ ] **Step 1: 写失败测试（区块写入逻辑，PDF 读取打桩）**

```python
import sys, unittest, tempfile
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestUpsertSections(unittest.TestCase):
    def setUp(self):
        import extract_exam_text as e
        self.e = e

    def test_upsert_appends_when_absent(self):
        md = "---\n题号: 1\n---\n\n## 摘要\n一行\n"
        out = self.e.upsert_text_sections(md, stem="题干X", sol="解析Y")
        self.assertIn("## 题干文本\n题干X", out)
        self.assertIn("## 解析文本\n解析Y", out)
        self.assertIn("## 摘要\n一行", out)        # 不破坏既有

    def test_upsert_idempotent_replaces_block(self):
        md = "## 题干文本\n旧\n\n## 解析文本\n旧\n"
        out = self.e.upsert_text_sections(md, stem="新题干", sol="新解析")
        self.assertIn("## 题干文本\n新题干", out)
        self.assertNotIn("旧", out)
        self.assertEqual(out.count("## 题干文本"), 1)   # 不重复追加


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_extract_exam_text -v`
Expected: FAIL（`No module named extract_exam_text`）

- [ ] **Step 3: 实现**

```python
# extract_exam_text.py
"""P0：从解析卷 PDF 按题抽题干/解析文本，写回真题 md 的「## 题干文本」「## 解析文本」。

CLI:
  python extract_exam_text.py --province 吉林 --subject 数学            # dry-run
  python extract_exam_text.py --province 吉林 --subject 数学 --apply
  python extract_exam_text.py --province 吉林 --subject 数学 --apply --force  # 覆盖已抽
"""
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

import pypdf

sys.path.insert(0, str(Path(__file__).parent))
from _utils import REPO_ROOT, read_frontmatter, setup_utf8  # noqa: E402
from exam_text_segment import segment_by_qno  # noqa: E402

STEM_START, STEM_END = "<!-- 题干文本-start -->", "<!-- 题干文本-end -->"
SOL_START, SOL_END = "<!-- 解析文本-start -->", "<!-- 解析文本-end -->"
_BLOCK = "{s}\n## {title}\n{body}\n{e}"


def _sub_block(text: str, start: str, end: str, title: str, body: str) -> str:
    seg = _BLOCK.format(s=start, title=title, body=body, e=end)
    pat = re.compile(re.escape(start) + r".*?" + re.escape(end), re.S)
    if start in text:
        return pat.sub(seg, text)
    return text.rstrip() + "\n\n" + seg + "\n"


def upsert_text_sections(md: str, *, stem: str, sol: str) -> str:
    md = _sub_block(md, STEM_START, STEM_END, "题干文本", stem)
    md = _sub_block(md, SOL_START, SOL_END, "解析文本", sol)
    return md


def read_pdf_page(pdf_rel: str, page: int) -> str:
    path = REPO_ROOT / pdf_rel
    reader = pypdf.PdfReader(str(path))
    if not (1 <= page <= len(reader.pages)):
        return ""
    return reader.pages[page - 1].extract_text() or ""


def main() -> int:
    setup_utf8()
    ap = argparse.ArgumentParser()
    ap.add_argument("--province", default="吉林")
    ap.add_argument("--subject", default="数学")
    ap.add_argument("--apply", action="store_true")
    ap.add_argument("--force", action="store_true", help="覆盖已抽取的题")
    args = ap.parse_args()

    exam_dir = REPO_ROOT / "真题" / f"{args.province}-{args.subject}"
    paths = sorted(exam_dir.glob("*.md"))
    done = fail = skip = 0
    fails: list[str] = []
    for p in paths:
        text = p.read_text(encoding="utf-8")
        if STEM_START in text and not args.force:
            skip += 1
            continue
        fm = read_frontmatter(p)
        pdf_rel, page, qno = fm.get("PDF"), fm.get("PDF页码"), fm.get("题号")
        if not (pdf_rel and page and qno):
            fail += 1
            fails.append(f"{p.name}: 缺 PDF/页码/题号")
            continue
        try:
            page_text = read_pdf_page(str(pdf_rel), int(page))
        except (OSError, ValueError, pypdf.errors.PdfReadError) as ex:
            fail += 1
            fails.append(f"{p.name}: PDF 读取失败 {ex}")
            continue
        seg = segment_by_qno(page_text, int(qno))
        if seg["fallback"]:
            fails.append(f"{p.name}: 切分回退（整页入解析）")
        new = upsert_text_sections(text, stem=seg["题干"], sol=seg["解析"])
        if args.apply and new != text:
            p.write_text(new, encoding="utf-8")
        done += 1
    print(f"{'[APPLY]' if args.apply else '[dry-run]'} 处理 {done} / 跳过 {skip} / 失败 {fail}"
          f"（{exam_dir.name}）")
    for f in fails[:40]:
        print("  ⚠", f)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 运行验证通过**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_extract_exam_text -v`
Expected: PASS（2 tests）

- [ ] **Step 5: 全量单测回归 + 提交**

```bash
cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest discover -s tests 2>&1 | grep -E "^Ran|^OK|^FAILED"
git add 00-元/scripts/extract_exam_text.py 00-元/scripts/tests/test_extract_exam_text.py
git commit -m "feat(review): extract_exam_text 抽解析卷文本写回真题 md（幂等/dry-run）"
```

### Task P0.3: 数学语料抽取 + 质量验证（人工检查点，非 TDD）

**Files:** `真题/吉林-数学/*.md`、`真题/北京-数学/*.md`、`真题/湖南-数学/*.md`

- [ ] **Step 1: dry-run 看切分成功率**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python extract_exam_text.py --province 吉林 --subject 数学`
Expected: 打印「处理 N / 跳过 / 失败」+ 回退清单。记录回退率。

- [ ] **Step 2: 抽样核对公式抽取质量**

随机读 5 道已 dry-run 的真题对应 PDF 页文本，确认题干/解析切分正确、公式（虽可能为线性化文本）可读。**这是最大技术风险点**：若公式抽取严重不可读（大量乱码/丢符号），在 spec 风险栏走视觉校正升级（暂记 backlog，不阻塞——知识精要/方法可不依赖公式精确字符）。

- [ ] **Step 3: 调切分锚点（如回退率 > 20%）**

若回退率高：检查 `exam_text_segment._qno_anchor` 正则与真实 PDF 题号格式（可能是 `（1）`/`一、1`），按真实样式补正则分支，回到 Task P0.1 加测试用例再实现。

- [ ] **Step 4: apply 三省数学 + 提交**

```bash
cd 00-元/scripts
for prov in 吉林 北京 湖南; do PYTHONIOENCODING=utf-8 python extract_exam_text.py --province $prov --subject 数学 --apply; done
cd ../.. && git add 真题/吉林-数学 真题/北京-数学 真题/湖南-数学
git commit -m "data: 数学真题文本语料抽取（题干/解析文本回填）"
```

---

## P1 — 数学考点专题闭环

### Task P1.1: 考点聚合纯函数

**Files:**
- Create: `00-元/scripts/kaodian_aggregate.py`
- Test: `00-元/scripts/tests/test_kaodian_aggregate.py`

- [ ] **Step 1: 写失败测试**

```python
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestAggregate(unittest.TestCase):
    def setUp(self):
        import kaodian_aggregate as k
        self.k = k

    def test_build_kaodian_map_groups_and_sorts(self):
        atoms = [
            {"_bare": "2024-A-01", "考点": ["复数", "模长"], "年份": "2024",
             "难度": "易", "题型": "选择", "题号": "1", "摘要": "求模"},
            {"_bare": "2010-A-02", "考点": ["复数"], "年份": "2010",
             "难度": "中", "题型": "选择", "题号": "2", "摘要": "复数运算"},
        ]
        group = {"复数": "复数", "模长": "复数"}
        m = self.k.build_kaodian_map(atoms, group)
        self.assertEqual(m["复数"]["父主题"], "复数")
        self.assertEqual(m["复数"]["真题数"], 2)
        # 代表题近年优先：2024 排前
        self.assertEqual(m["复数"]["真题"][0]["_bare"], "2024-A-01")
        self.assertIn(("选择", 1), m["复数"]["题位"])

    def test_only_kaodian_with_atoms(self):
        m = self.k.build_kaodian_map([], {"复数": "复数"})
        self.assertEqual(m, {})


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_kaodian_aggregate -v`
Expected: FAIL（`No module named kaodian_aggregate`）

- [ ] **Step 3: 实现**

```python
# kaodian_aggregate.py
"""聚合 考点 → {父主题, 真题(按年份降序), 真题数, 题位, 难度分布, 年份跨度}。纯函数。"""
from __future__ import annotations

import collections


def _year(a: dict) -> int:
    try:
        return int(a.get("年份") or 0)
    except ValueError:
        return 0


def build_kaodian_map(atoms: list[dict], group: dict[str, str]) -> dict[str, dict]:
    """atoms: 真题 frontmatter 列表（含 _bare/考点/年份/难度/题型/题号/摘要）。
    返回仅含有 ≥1 真题的考点。"""
    by: dict[str, list[dict]] = collections.defaultdict(list)
    for a in atoms:
        for kp in a.get("考点", []) or []:
            by[kp].append(a)
    out: dict[str, dict] = {}
    for kp, items in by.items():
        items = sorted(items, key=lambda x: -_year(x))
        diffs = collections.Counter(x.get("难度", "") for x in items)
        qpos = sorted({(x.get("题型", ""), int(x.get("题号") or 0)) for x in items},
                      key=lambda t: (t[0], t[1]))
        years = [_year(x) for x in items if _year(x)]
        out[kp] = {
            "父主题": group.get(kp, "其他"),
            "真题": items,
            "真题数": len(items),
            "题位": qpos,
            "难度分布": dict(diffs),
            "年份跨度": (min(years), max(years)) if years else (0, 0),
        }
    return out
```

- [ ] **Step 4: 运行验证通过**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_kaodian_aggregate -v`
Expected: PASS（2 tests）

- [ ] **Step 5: 提交**

```bash
git add 00-元/scripts/kaodian_aggregate.py 00-元/scripts/tests/test_kaodian_aggregate.py
git commit -m "feat(review): 考点聚合纯函数（按年份降序/题位/难度分布）"
```

### Task P1.2: 提示词模板 + prompt 构造

**Files:**
- Create: `00-元/scripts/_prompts/kaodian_review.md`
- Modify: `00-元/scripts/gen_kaodian_review.py`（本任务先建文件 + `build_prompt`）
- Test: `00-元/scripts/tests/test_gen_kaodian_review.py`

- [ ] **Step 1: 写提示词模板**

```bash
cat > 00-元/scripts/_prompts/kaodian_review.md <<'PROMPT'
你是高考{subject}复习资料编辑。下面给定一个考点及其全部真题的题干/解析文本。
请输出该考点的复习专题正文（Markdown，简体中文，知识密集、面向考生应试）。

【硬约束】
- 数学/物理公式必须用 Obsidian 定界符：行内 $...$、块级 $$...$$。严禁 \( \) \[ \]。
- 不要输出 frontmatter、不要代码围栏、不要复述本提示词。
- 严格只输出下列 4 个二级标题区块，标题文字一字不差：

## 知识精要
（考纲级核心：定义/公式/定理/适用条件，从真题解析提炼）

## 解题方法与套路
（跨这些真题归纳的通用解法步骤，编号列出）

## 高频易错
（这些真题暴露的典型陷阱/失分点，要点列出）

## 代表题精讲
（对下方标【代表题】的 2-3 道，逐题：**题目**简述→**思路**→**步骤**→**点评/变式**）

【考点】{kaodian}（父主题：{parent}；共 {n} 道真题；年份 {y0}–{y1}）

【真题语料】
{corpus}
PROMPT
```

- [ ] **Step 2: 写失败测试（prompt 构造）**

```python
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestBuildPrompt(unittest.TestCase):
    def setUp(self):
        import gen_kaodian_review as g
        self.g = g

    def test_build_prompt_fills_template_and_marks_reps(self):
        info = {
            "父主题": "复数", "真题数": 2, "年份跨度": (2010, 2024),
            "真题": [
                {"_bare": "2024-A-01", "年份": "2024", "题干文本": "求|z|",
                 "解析文本": "解：|z|=√2"},
                {"_bare": "2010-A-02", "年份": "2010", "题干文本": "复数运算",
                 "解析文本": "解：..."},
            ],
        }
        p = self.g.build_prompt("数学", "复数", info, rep_n=1)
        self.assertIn("## 代表题精讲", p)
        self.assertIn("复数", p)
        self.assertIn("【代表题】", p)            # 近年第一题被标代表题
        self.assertIn("2024-A-01", p)
        self.assertNotIn("\\(", p)                 # 模板无 LaTeX 圆括号定界符


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_gen_kaodian_review -v`
Expected: FAIL（`No module named gen_kaodian_review`）

- [ ] **Step 4: 实现 build_prompt（先建 gen_kaodian_review.py 骨架）**

```python
# gen_kaodian_review.py（本任务仅 build_prompt + 模板加载；主流程下个任务）
from __future__ import annotations

import re
from pathlib import Path

_PROMPT = Path(__file__).parent / "_prompts" / "kaodian_review.md"


def _load_template() -> str:
    return _PROMPT.read_text(encoding="utf-8")


def build_prompt(subject: str, kaodian: str, info: dict, rep_n: int = 3) -> str:
    items = info["真题"]
    rep_ids = {it["_bare"] for it in items[:rep_n]}   # 近年优先（已降序）
    blocks = []
    for it in items:
        mark = "【代表题】" if it["_bare"] in rep_ids else ""
        blocks.append(
            f"### {mark}{it['_bare']}（{it.get('年份','')}）\n"
            f"题干：{it.get('题干文本','') or it.get('摘要','')}\n"
            f"解析：{it.get('解析文本','')}"
        )
    corpus = "\n\n".join(blocks)
    y0, y1 = info["年份跨度"]
    return _load_template().format(
        subject=subject, kaodian=kaodian, parent=info["父主题"],
        n=info["真题数"], y0=y0, y1=y1, corpus=corpus,
    )
```

- [ ] **Step 5: 运行验证通过 + 提交**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_gen_kaodian_review -v`
Expected: PASS（1 test）

```bash
git add 00-元/scripts/_prompts/kaodian_review.md 00-元/scripts/gen_kaodian_review.py 00-元/scripts/tests/test_gen_kaodian_review.py
git commit -m "feat(review): 考点专题提示词模板 + build_prompt（近年代表题标记）"
```

### Task P1.3: gen_kaodian_review.py 主流程

**Files:**
- Modify: `00-元/scripts/gen_kaodian_review.py`
- Test: `00-元/scripts/tests/test_gen_kaodian_review.py`（加 render_md 测试）

- [ ] **Step 1: 加失败测试（render_md：拼定位/清单/关联，不依赖 LLM）**

```python
    def test_render_md_has_static_blocks(self):
        import gen_kaodian_review as g
        info = {"父主题": "复数", "真题数": 1, "题位": [("选择", 1)],
                "难度分布": {"易": 1}, "年份跨度": (2024, 2024),
                "真题": [{"_bare": "2024-A-01", "年份": "2024", "难度": "易",
                          "摘要": "求模"}]}
        md = g.render_md("数学", "复数", info, llm_body="## 知识精要\n模长公式",
                         weight=12, concept_link="[[330-复数概念|复数]]")
        self.assertIn("考点: 复数", md)            # frontmatter
        self.assertIn("## 考点定位", md)
        self.assertIn("## 知识精要", md)           # LLM 段嵌入
        self.assertIn("## 全部真题清单", md)
        self.assertIn("[[2024-A-01", md)           # 真题链接
        self.assertIn("## 关联", md)
        self.assertIn("[[330-复数概念|复数]]", md)
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_gen_kaodian_review -v`
Expected: FAIL（`render_md` 不存在）

- [ ] **Step 3: 实现 render_md + main**

```python
# 追加到 gen_kaodian_review.py
import argparse
import sys

sys.path.insert(0, str(Path(__file__).parent))
from _utils import REPO_ROOT, read_frontmatter, setup_utf8  # noqa: E402
from _llm_router import call, Task  # noqa: E402
from kaodian_aggregate import build_kaodian_map  # noqa: E402

import yaml

GROUP = lambda subj: Path(__file__).parent / f"group_考点_{subj}.yaml"


def _safe_name(s: str) -> str:
    return re.sub(r'[\\/:*?"<>|]', "_", s)


def render_md(subject: str, kaodian: str, info: dict, *, llm_body: str,
              weight: int, concept_link: str) -> str:
    y0, y1 = info["年份跨度"]
    pos = " ".join(f"{t}{n}" for t, n in info["题位"])
    diff = " ".join(f"{k}{v}" for k, v in info["难度分布"].items())
    fm = (f"---\ntitle: {kaodian}\n考点: {kaodian}\n父主题: {info['父主题']}\n"
          f"学科: {subject}\nweight: {weight}\n真题数: {info['真题数']}\n"
          f"高频题位: [{', '.join(f'{t}{n}' for t, n in info['题位'])}]\n状态: 草稿\n---\n")
    定位 = (f"\n## 考点定位\n\n- 父主题：{info['父主题']}\n- 权重 weight：{weight}\n"
            f"- 真题数：{info['真题数']}；年份跨度：{y0}–{y1}\n"
            f"- 出现题位：{pos}\n- 难度分布：{diff}\n")
    清单 = ["\n## 全部真题清单\n"]
    for it in info["真题"]:
        清单.append(f"- [[{it['_bare']}]]（{it.get('年份','')} · {it.get('难度','')}）"
                    f" {it.get('摘要','')}")
    关联 = f"\n\n## 关联\n\n- 概念词条：{concept_link or '（暂无对应词条）'}\n"
    return fm + 定位 + "\n" + llm_body.strip() + "\n" + "\n".join(清单) + 关联


def main() -> int:
    setup_utf8()
    ap = argparse.ArgumentParser()
    ap.add_argument("--province", default="吉林")
    ap.add_argument("--subject", default="数学")
    ap.add_argument("--考点", dest="one")
    ap.add_argument("--limit", type=int)
    ap.add_argument("--apply", action="store_true")
    ap.add_argument("--regen", action="store_true", help="草稿也重生")
    args = ap.parse_args()

    exam_dir = REPO_ROOT / "真题" / f"{args.province}-{args.subject}"
    atoms = []
    for p in sorted(exam_dir.glob("*.md")):
        fm = read_frontmatter(p)
        fm["_bare"] = p.stem
        # 题干/解析文本从正文区块拿（read_frontmatter 仅 fm；这里补读正文）
        body = p.read_text(encoding="utf-8")
        for tag, key in (("题干文本", "题干文本"), ("解析文本", "解析文本")):
            m = re.search(rf"## {tag}\n(.*?)(?:\n<!--|\n## |\Z)", body, re.S)
            fm[key] = m.group(1).strip() if m else ""
        atoms.append(fm)

    group = {str(k): str(v) for k, v in
             (yaml.safe_load(GROUP(args.subject).read_text(encoding="utf-8")) or {}).items()}
    kmap = build_kaodian_map(atoms, group)
    if args.one:
        kmap = {args.one: kmap[args.one]} if args.one in kmap else {}

    out_dir = REPO_ROOT / "复习" / args.subject
    out_dir.mkdir(parents=True, exist_ok=True)
    items = list(kmap.items())
    if args.limit:
        items = items[: args.limit]
    done = skip = 0
    for kp, info in items:
        fpath = out_dir / f"{_safe_name(kp)}.md"
        if fpath.exists() and not args.regen:
            existing = fpath.read_text(encoding="utf-8")
            if "状态: 已校对" in existing or not args.regen:
                skip += 1
                continue
        prompt = build_prompt(args.subject, kp, info)
        try:
            body = call(prompt=prompt, task=Task.COMPLEX,
                        system=f"你是高考{args.subject}复习资料编辑，输出知识密集的应试复习正文。").text
        except Exception as ex:  # noqa: BLE001
            print(f"  ⚠ {kp}: LLM 失败 {ex}")
            continue
        md = render_md(args.subject, kp, info, llm_body=body, weight=0, concept_link="")
        if args.apply:
            fpath.write_text(md, encoding="utf-8")
        done += 1
        print(f"  ✓ {kp}（{info['真题数']}题）")
    print(f"{'[APPLY]' if args.apply else '[dry-run]'} 生成 {done} / 跳过 {skip}（复习/{args.subject}）")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

> 注：`weight`/`concept_link` 在此版先置默认（0/空）；Task P1.5 再从概念词条 frontmatter + `fix_wikilinks` alias 查找回填，保持本任务可独立通过测试。

- [ ] **Step 4: 运行验证通过 + 回归**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_gen_kaodian_review -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add 00-元/scripts/gen_kaodian_review.py 00-元/scripts/tests/test_gen_kaodian_review.py
git commit -m "feat(review): gen_kaodian_review 主流程（聚合→LLM→复习 md）"
```

### Task P1.4: validate_review.py 体检门

**Files:**
- Create: `00-元/scripts/validate_review.py`
- Test: `00-元/scripts/tests/test_validate_review.py`

- [ ] **Step 1: 写失败测试**

```python
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestValidateReview(unittest.TestCase):
    def setUp(self):
        import validate_review as v
        self.v = v

    def test_clean_doc_passes(self):
        md = ("---\n考点: 复数\n父主题: 复数\n学科: 数学\n状态: 草稿\n---\n"
              "## 考点定位\nx\n## 知识精要\n模长 $|z|$\n## 解题方法与套路\nx\n"
              "## 高频易错\nx\n## 代表题精讲\nx\n## 全部真题清单\n- [[a]]\n## 关联\nx\n")
        self.assertEqual(self.v.check(md), [])

    def test_flags_latex_delim_and_missing_block(self):
        md = ("---\n考点: 复数\n父主题: 复数\n学科: 数学\n状态: 草稿\n---\n"
              "## 考点定位\n模长 \\(|z|\\)\n## 知识精要\nx\n## 高频易错\nx\n"
              "## 代表题精讲\nx\n## 全部真题清单\nx\n## 关联\nx\n")
        errs = self.v.check(md)
        self.assertTrue(any("LaTeX 定界符" in e for e in errs))
        self.assertTrue(any("缺区块" in e and "解题方法与套路" in e for e in errs))

    def test_flags_code_fence(self):
        md = ("---\n考点: x\n父主题: y\n学科: 数学\n状态: 草稿\n---\n"
              "## 考点定位\n```\n围栏\n```\n## 知识精要\nx\n## 解题方法与套路\nx\n"
              "## 高频易错\nx\n## 代表题精讲\nx\n## 全部真题清单\nx\n## 关联\nx\n")
        self.assertTrue(any("代码围栏" in e for e in self.v.check(md)))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_validate_review -v`
Expected: FAIL（`No module named validate_review`）

- [ ] **Step 3: 实现**

```python
# validate_review.py
"""复习 md 体检门：缺区块/缺字段/代码围栏/LaTeX 定界符/wikilinks。

CLI: python validate_review.py --subject 数学   # 扫 复习/<科>/，非0退出码=有故障
"""
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
from _utils import REPO_ROOT, setup_utf8  # noqa: E402

_REQ_FIELDS = ("考点:", "父主题:", "学科:", "状态:")
_REQ_BLOCKS = ("## 考点定位", "## 知识精要", "## 解题方法与套路",
               "## 高频易错", "## 代表题精讲", "## 全部真题清单", "## 关联")
_LATEX = re.compile(r"\\\(|\\\)|\\\[|\\\]")


def check(md: str) -> list[str]:
    errs: list[str] = []
    fm = md.split("---", 2)
    head = fm[1] if len(fm) >= 3 else ""
    for f in _REQ_FIELDS:
        if f not in head:
            errs.append(f"缺 frontmatter 字段：{f}")
    for b in _REQ_BLOCKS:
        if b not in md:
            errs.append(f"缺区块：{b}")
    if "```" in md:
        errs.append("含代码围栏 ```")
    if _LATEX.search(md):
        errs.append("含 LaTeX 定界符 \\(\\)\\[\\]（须改 $...$）")
    return errs


def main() -> int:
    setup_utf8()
    ap = argparse.ArgumentParser()
    ap.add_argument("--subject", default="数学")
    args = ap.parse_args()
    d = REPO_ROOT / "复习" / args.subject
    bad = 0
    for p in sorted(d.glob("*.md")):
        errs = check(p.read_text(encoding="utf-8"))
        if errs:
            bad += 1
            print(f"✗ {p.name}")
            for e in errs:
                print(f"    - {e}")
    print(f"体检：{bad} 个文件有故障（复习/{args.subject}）")
    return 1 if bad else 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 运行验证通过 + 提交**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_validate_review -v`
Expected: PASS（3 tests）

```bash
git add 00-元/scripts/validate_review.py 00-元/scripts/tests/test_validate_review.py
git commit -m "feat(review): validate_review 体检门（区块/字段/围栏/公式定界符）"
```

### Task P1.5: weight + 概念词条链接回填

**Files:**
- Modify: `00-元/scripts/gen_kaodian_review.py:main`（回填 weight/concept_link）
- Test: `00-元/scripts/tests/test_gen_kaodian_review.py`（加 resolve_concept 测试）

- [ ] **Step 1: 加失败测试**

```python
    def test_resolve_concept_uses_alias_lookup(self):
        import gen_kaodian_review as g
        alias = {"余弦定理": "126-定理"}
        link, weight = g.resolve_concept("余弦定理", alias,
                                         weights={"126-定理": 87})
        self.assertEqual(link, "[[126-定理|余弦定理]]")
        self.assertEqual(weight, 87)

    def test_resolve_concept_missing(self):
        import gen_kaodian_review as g
        link, weight = g.resolve_concept("无词条考点", {}, weights={})
        self.assertEqual(link, "")
        self.assertEqual(weight, 0)
```

- [ ] **Step 2: 运行验证失败**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest tests.test_gen_kaodian_review -v`
Expected: FAIL（`resolve_concept` 不存在）

- [ ] **Step 3: 实现 resolve_concept + 在 main 接线**

```python
# 追加到 gen_kaodian_review.py
def resolve_concept(kaodian: str, alias_lookup: dict[str, str],
                    weights: dict[str, str | int]) -> tuple[str, int]:
    """考点 → (wikilink 到概念词条, weight)。无对应词条返回 ("", 0)。"""
    stem = alias_lookup.get(kaodian)
    if not stem:
        return "", 0
    try:
        w = int(weights.get(stem, 0) or 0)
    except ValueError:
        w = 0
    return f"[[{stem}|{kaodian}]]", w
```

在 `main` 里，循环前构造 `alias_lookup` 与 `weights`，并把生成调用改为传真实值：

```python
    from fix_wikilinks import collect_targets
    _, alias_lookup = collect_targets()
    weights: dict[str, int] = {}
    subj_dir = REPO_ROOT / args.subject
    if subj_dir.is_dir():
        for cp in subj_dir.glob("*.md"):
            wf = read_frontmatter(cp)
            if wf.get("weight"):
                weights[cp.stem] = wf["weight"]
    # ...循环内：
    #   concept_link, weight = resolve_concept(kp, alias_lookup, weights)
    #   md = render_md(args.subject, kp, info, llm_body=body, weight=weight,
    #                  concept_link=concept_link)
```

- [ ] **Step 4: 运行验证通过 + 回归 + 提交**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest discover -s tests 2>&1 | grep -E "^Ran|^OK|^FAILED"`
Expected: OK

```bash
git add 00-元/scripts/gen_kaodian_review.py 00-元/scripts/tests/test_gen_kaodian_review.py
git commit -m "feat(review): 回填考点→概念词条链接与 weight"
```

### Task P1.6: gen_html.py 渲染复习树

**Files:**
- Modify: `00-元/scripts/gen_html.py`
- Test: `00-元/scripts/tests/test_gen_html.py`（若不存在则新建该测试文件）

- [ ] **Step 1: 先读 gen_html.py 摸清现有渲染入口**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python gen_html.py --help`
然后 `grep -nE "def |glob|atoms|学科|render|首页" gen_html.py | head -40` 找到「遍历 md → 渲染 HTML」的主循环与目录清单常量。

- [ ] **Step 2: 加失败测试（复习目录被纳入渲染清单）**

```python
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))


class TestGenHtmlReview(unittest.TestCase):
    def test_review_dir_in_source_dirs(self):
        import gen_html as h
        dirs = h.source_dirs()  # 现有列出待渲染顶层目录的函数（若名不同，按实际改）
        self.assertTrue(any(str(d).endswith("复习") or d.name == "复习" for d in dirs))


if __name__ == "__main__":
    unittest.main()
```

> 若 `gen_html.py` 没有 `source_dirs()` 这样的可测函数，先做一次小重构：把「待渲染目录清单」抽成一个返回 `list[Path]` 的函数，再让主流程调用它（重构不改行为，跑现有 HTML 应字节不变）。

- [ ] **Step 3: 运行验证失败 → 实现（把 `复习` 加入目录清单 + 复用现有 md→HTML 渲染）**

把 `REPO_ROOT/"复习"` 加入待渲染顶层目录；复习 md 的渲染复用既有学科词条渲染路径（KaTeX 公式、wikilinks 解析、目录树）。无需新模板。

- [ ] **Step 4: 验证通过 + 数学 HTML 字节不变（向后兼容）**

```bash
cd 00-元/scripts && PYTHONIOENCODING=utf-8 python -m unittest discover -s tests 2>&1 | grep -E "^Ran|^OK|^FAILED"
PYTHONIOENCODING=utf-8 python gen_html.py --apply
cd ../.. && git status --short docs/student/数学题位速查.html   # 应为空（不受影响）
```

- [ ] **Step 5: 提交**

```bash
git add 00-元/scripts/gen_html.py 00-元/scripts/tests/test_gen_html.py
git commit -m "feat(review): gen_html 渲染复习考点专题树"
```

### Task P1.7: 数学全考点专题生成 + 校对 + 落库（人工检查点）

**Files:** `复习/数学/*.md`、`docs/student/`

- [ ] **Step 1: 先小批 dry-run/试跑（高频考点）**

Run: `cd 00-元/scripts && PYTHONIOENCODING=utf-8 python gen_kaodian_review.py --subject 数学 --limit 5 --apply`
读 3 个产物，确认 4 个 LLM 区块质量、公式定界符、代表题选取近年优先正确。

- [ ] **Step 2: 体检门**

Run: `PYTHONIOENCODING=utf-8 python validate_review.py --subject 数学`
有故障 → 看清单；公式定界符问题跑 `python fix_latex_delim.py --apply`（按 manifest 限 `复习/数学/`），链接问题跑 `python fix_wikilinks.py --apply`。

- [ ] **Step 3: 全量生成数学**

Run: `PYTHONIOENCODING=utf-8 python gen_kaodian_review.py --subject 数学 --apply`
再跑体检门直到 0 故障；`python fix_wikilinks.py --apply`。

- [ ] **Step 4: 生成 HTML + 单测回归**

```bash
cd 00-元/scripts && PYTHONIOENCODING=utf-8 python gen_html.py --apply
PYTHONIOENCODING=utf-8 python -m unittest discover -s tests 2>&1 | grep -E "^Ran|^OK|^FAILED"
```

- [ ] **Step 5: 提交数学复习材料**

```bash
cd ../.. && git add 复习/数学 docs/student
git commit -m "data: 数学考点专题复习材料生成（全考点）+ HTML"
```

---

## P2 — 扩科（物理 → 化学 → 生物，工具已参数化，按 recipe 复用）

> 每科执行：①`extract_exam_text.py --province {省} --subject {科} --apply`（吉林/北京/湖南）→ 抽样验证公式抽取质量（物理公式风险最高）；②`gen_kaodian_review.py --subject {科} --apply` → `validate_review.py` → `fix_latex_delim`/`fix_wikilinks`；③`gen_html.py --apply`；④单测回归 + 分科提交。物理公式若抽取严重不可读，按 spec 风险栏对代表题做视觉校正（升级 backlog），不阻塞知识精要/方法/易错。

- [ ] **Task P2.1: 物理** — 文本语料 + 全考点专题 + HTML + 提交。
- [ ] **Task P2.2: 化学** — 同上。
- [ ] **Task P2.3: 生物** — 同上。

---

## Self-Review

**Spec 覆盖：**
- 三层流水线 → P0（语料层 P0.1-3）/ P1（生成层 P1.1-5 + 体检 P1.4）/ 消费层（P1.6 gen_html）✅
- 7 区块（定位/知识精要/方法/易错/代表题/真题清单/关联）→ render_md（静态 4 块）+ prompt 模板（LLM 4 块）+ validate_review 校 7 块齐 ✅
- 关键映射（考点→真题/父主题/题位/词条）→ kaodian_aggregate + resolve_concept ✅
- 代表题近年优先 → build_kaodian_map 按年份降序 + build_prompt 取前 rep_n 标记 ✅
- 全考点（有 ≥1 真题）→ build_kaodian_map 仅含有真题的考点 ✅
- 公式定界符/wikilinks 红线 → 模板硬约束 + validate_review + fix_latex_delim/fix_wikilinks hook ✅
- 分期 P0→P1→P2、数学先行 ✅
- PDF 公式抽取风险（最大）→ P0.3 验证检查点 + 视觉校正 backlog ✅

**占位扫描：** 各步含真实代码/命令/期望；P1.6 因依赖现有 gen_html 内部结构，给了「先探查再按实际函数名接线」的明确指引（含若无可测函数则先小重构），非空泛占位。

**类型一致：** `build_kaodian_map`→`info` 字段（父主题/真题/真题数/题位/难度分布/年份跨度）在 build_prompt/render_md 一致；`resolve_concept(kaodian, alias_lookup, weights)→(link,weight)` 与 main 接线一致；真题 `_bare` 贯穿聚合/prompt/清单一致；区块标题文字在 模板/render_md/validate_review 三处一字一致。
