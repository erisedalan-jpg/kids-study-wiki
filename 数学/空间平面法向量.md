---
title: 空间平面法向量
aliases: [空间平面法向量, normal vector, 法向量]
学科: 数学
学段: [高中]
主题: [选择性必修一, 第一章空间向量与立体几何]
状态: 全龄完成
英文术语: normal vector of plane
首次共读:
最近共读:
---

## 🎓 空间平面法向量

### 定义

**法向量**：若非零向量 $\boldsymbol{n}$ 与平面 $\alpha$ **垂直**，则称 $\boldsymbol{n}$ 为平面 $\alpha$ 的**法向量**。

法向量不唯一，$\lambda\boldsymbol{n}$（$\lambda\neq0$）都是 $\alpha$ 的法向量。

### 法向量的求法

**已知平面内两个不共线向量 $\boldsymbol{a},\boldsymbol{b}**，法向量 $\boldsymbol{n}=(x,y,z)$ 满足：

$$\boldsymbol{n}\cdot\boldsymbol{a} = 0 \quad \text{且} \quad \boldsymbol{n}\cdot\boldsymbol{b} = 0$$

即解**齐次方程组**（两个方程，三个未知数，取一组特解）。

**步骤**：
1. 在平面内取两个不共线向量 $\boldsymbol{a},\boldsymbol{b}$
2. 设 $\boldsymbol{n}=(x,y,z)$，列 $\boldsymbol{n}\cdot\boldsymbol{a}=0$ 和 $\boldsymbol{n}\cdot\boldsymbol{b}=0$ 两个方程
3. 令 $z=1$（或其他非零值），解出 $x,y$

### 法向量的应用

| 应用 | 公式 |
|------|------|
| 两平面所成角（二面角） | $\cos\theta = \dfrac{|\boldsymbol{n}_1\cdot\boldsymbol{n}_2|}{|\boldsymbol{n}_1||\boldsymbol{n}_2|}$ |
| 线面角（线与面所成角） | $\sin\varphi = \dfrac{|\boldsymbol{v}\cdot\boldsymbol{n}|}{|\boldsymbol{v}||\boldsymbol{n}|}$ |
| 点到平面距离 | $d = \dfrac{|\overrightarrow{A_0P}\cdot\boldsymbol{n}|}{|\boldsymbol{n}|}$ |
| 两平面平行 | $\boldsymbol{n}_1 \parallel \boldsymbol{n}_2$ |
| 两平面垂直 | $\boldsymbol{n}_1 \cdot \boldsymbol{n}_2 = 0$ |

### 典型例题

**例1**：正方体 $ABCD$-$A_1B_1C_1D_1$（棱长1），以 $A$ 为原点建坐标系，求平面 $ABB_1A_1$ 的法向量。

**解**：$A(0,0,0)$，$B(1,0,0)$，$A_1(0,0,1)$。

平面内向量：$\overrightarrow{AB}=(1,0,0)$，$\overrightarrow{AA_1}=(0,0,1)$。

设 $\boldsymbol{n}=(x,y,z)$：
$$\boldsymbol{n}\cdot\overrightarrow{AB} = x = 0$$
$$\boldsymbol{n}\cdot\overrightarrow{AA_1} = z = 0$$

取 $y=1$，得 $\boldsymbol{n}=(0,1,0)$（即 $y$ 轴方向，合理：$ABB_1A_1$ 是 $xOz$ 平面）。

**例2**：在正三棱锥 $P$-$ABC$ 中，底面 $ABC$ 是边长为 2 的正三角形，高为 $\sqrt{6}$，以 $A$ 为原点，$AB$ 方向为 $x$ 轴建坐标，求平面 $PAB$ 的法向量。

**解**：
$A(0,0,0)$，$B(2,0,0)$，$C(1,\sqrt{3},0)$，$O'$ 为底面中心 $\left(1,\dfrac{\sqrt{3}}{3}\cdot\sqrt{3},0\right)=\left(1,1,0\right)$（待校验）... （此处坐标建立需按实际图形精确计算，结论形式为 $\boldsymbol{n}\cdot\overrightarrow{AB}=0$ 且 $\boldsymbol{n}\cdot\overrightarrow{AP}=0$）。

**例3**（关键方法）：已知平面 $\alpha$ 内有向量 $\boldsymbol{a}=(1,1,0)$ 和 $\boldsymbol{b}=(0,1,1)$，求 $\alpha$ 的法向量。

**解**：设 $\boldsymbol{n}=(x,y,z)$：
$$x+y=0 \Rightarrow y=-x$$
$$y+z=0 \Rightarrow z=-y=x$$

取 $x=1$：$\boldsymbol{n}=(1,-1,1)$（可验证：$\boldsymbol{n}\cdot\boldsymbol{a}=1-1+0=0$，$\boldsymbol{n}\cdot\boldsymbol{b}=0-1+1=0$ ✓）

### 易错点

- 求法向量时至少需要平面内**两个不共线**向量
- 法向量方程组有无穷多解，**任取一个特解**即可，不需要化简成"最简"
- 法向量与直线方向向量的区别：法向量 $\perp$ 平面，方向向量 $\parallel$ 直线

---

## 📑 出处

- **课标**：普通高中数学课程标准（2017年版2020年修订），选择性必修第一册第一章
- **教材**：[[素材/教材/ChinaTextbook/高中/数学/人教版（A版）（主编：章建跃&李增沪）-人民教育出版社/普通高中教科书·数学（A版）选择性必修 第一册.pdf]]（第一章，待家长核对页码）
- **生成校对**：Claude 生成于 2026-05-09，由家长核对

## 🔗 相关词条

- [[空间向量数量积]]
- [[空间向量坐标表示]]
- [[空间向量证垂直]]
- [[空间向量求线面角]]
- [[空间向量求二面角]]
- [[空间向量求距离]]
- [[空间直线平面垂直]]
