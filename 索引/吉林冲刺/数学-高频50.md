---
title: 数学-高频50
学科: 数学
学段: [高中]
主题: [吉林冲刺, 仪表盘]
状态: 仪表盘
---

# 数学 · 高频 50 考点（吉林理科高中）

> 权重排序：吉林反链 × 5 + 学习路径 × 2 + alias × 1，仅高中、仅理科卷。
> 配置见 `00-元/scripts/weight_config.yaml`；重算：`python 00-元/scripts/compute_weight.py --apply`。
> **本视图仅显吉林命中**；其他省份视图各自仅显本省（如未来建 `北京冲刺/`）。

## Top 50

```dataview
TABLE
  weight AS "权",
  吉林反链 AS "吉",
  学习路径出现 AS "路径",
  alias_count AS "别名"
FROM "数学"
WHERE weight > 0
SORT weight DESC
LIMIT 50
```

## 全部（按权重）

```dataview
TABLE weight AS "权", 吉林反链 AS "吉"
FROM "数学"
WHERE weight > 0
SORT weight DESC
```

## 权重为 0 但有吉林文卷反链（保留位）

> 当前文卷权重 = 0；未来 yaml `文: 1.0` → 重跑即生效。

```dataview
TABLE 吉林反链 AS "吉总"
FROM "数学"
WHERE weight = 0 AND 吉林反链 > 0
SORT 吉林反链 DESC
LIMIT 30
```
