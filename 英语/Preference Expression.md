---
title: Preference Expression
aliases: [Preference Expression, Expressing Preferences]
学科: 英语
学段: [高中]
主题: []
状态: 骨架
英文术语: Expressing Preferences
---

# Preference Expression

> **一句话**：在英语中系统运用 `would rather`, `prefer`, `would prefer` 等结构表达偏好与取舍关系的语法与交际考点。
> **English**: The grammatical and communicative focus on using structures such as *would rather*, *prefer*, and *would prefer* to systematically express preferences and trade-offs in English.

## 🎓 考点精讲

- **定义/原理**：表达偏好（Preference）指说话人对两个或多个选项进行倾向性选择的语言功能。高考英语主要考查三类核心结构的选择与搭配：
  - **`prefer` 系列**：`prefer A to B` (名词/V-ing)、`prefer to do A rather than (to) do B`、`prefer doing A to doing B`。
  - **`would rather` 系列**：`would rather do A than (do) B` (同主语现在/将来)、`would rather (that) + 主语 + 过去式` (异主语现在/将来虚拟)、`would rather (that) + 主语 + had done` (异主语过去虚拟)。
  - **`would prefer` 系列**：`would prefer to do A rather than (to) do B`、`would prefer it if + 主语 + 过去式` (礼貌虚拟)。
  
- **关键方法**：解题时遵循“三步判定法”：
  1. **主语一致性判定**：同主语优先使用 `prefer to do / would rather do` 结构；异主语必须使用 `would rather + (that) 从句` 或 `would prefer it if` 结构。
  2. **时态与虚拟判定**：`would rather` 后接从句时，用一般过去式表示现在或将来的虚拟愿望，用过去完成时表示对过去的虚拟。
  3. **否定形式判定**：`would rather` 的否定在 `rather` 后加 `not` (`would rather not do`)，从句否定在谓语上加 `didn't` 或 `hadn't`。

- **典型应用**：高考常以语法填空、短文改错和情景交际题出现。
  - **场景一**：语法填空给出 `I would rather you ____ (stay) at home yesterday.`，答案应为 `had stayed`，考查异主语过去虚拟。
  - **场景二**：改错题中将 `He prefers to walk than to ride.` 改为 `He prefers to walk rather than ride.`，考查固定搭配的平行结构。

## ⚠️ 高频易错点

- **混淆 `prefer doing A to doing B` 与 `prefer to do A rather than do B`**：前者用 `to` 连接两个动名词，后者用 `rather than` 连接省略 `to` 的不定式，不可混搭 `prefer to do A to do B`。
- **`would rather` 后从句的时态误用**：错用现在时（如 `I would rather you go now.`）而忘记虚拟要求，正确形式为 `I would rather you went now.`。
- **否定词位置错误**：将 `would rather not do` 误写成 `would not rather do`；在从句中错把否定放在 `would rather` 前。
- **`prefer` 与 `rather than` 后的动词形式不一致**：未遵循“平行结构”原则，如写成 `prefer to go out rather than staying home`，应统一为不定式或动名词。

## 🌐 中英对照（术语表）

| 中文 | English | 说明 |
|------|---------|------|
| 表达偏好 | Expressing Preferences | 语言交际功能，表明选择倾向 |
| 宁愿/宁可 | would rather | 表示偏好，常与虚拟语气连用 |
| 更喜欢 | prefer | 表示一般或特定情况下的偏好 |
| 虚拟语气 | Subjunctive Mood | 用于表达与事实相反的假设或愿望 |
| 平行结构 | Parallel Structure | 要求并列成分保持相同语法形式 |

## 📑 出处与参考资料

- **教材**：(待主会话核对教材索引)
- **课标**：🚧
- **生成校对**：DeepSeek-complex 生成于 2026-05-16，由家长核对

## 🔗 相关考点

- [[Subjunctive Mood]]
- [[Verb Patterns (Gerund vs Infinitive)]]
- [[Sentence Correction Error Types]]
- [[Communicative Functions Invitations Suggestions]]
- [[Parallel Structure]]